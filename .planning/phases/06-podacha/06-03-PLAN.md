---
phase: 06-podacha
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [docs/demo-script.md]
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "Пользователь по покадровому чеклисту снимает демо 1-3 минуты без пропусков ключевых кадров"
    - "Чеклист требует запись без реальных секретов в кадре и покрывает Android-live + iOS-TestFlight-заметку"
  artifacts:
    - path: docs/demo-script.md
      provides: "Покадровый сценарий видео (7 тактов) + чеклист записи на русском"
      contains: "Connect"
      min_lines: 30
  key_links:
    - from: docs/demo-script.md
      to: README.md
      via: "видео из сценария демонстрирует поведение, описанное в README (Connect->статусы->Disconnect)"
      pattern: "Disconnect"
---

<objective>
Написать docs/demo-script.md — покадровый сценарий и чеклист для записи демо-видео 1-3 минуты, по которому пользователь снимет полный путь Connect -> статусы/логи/трафик -> Disconnect. Агент готовит сценарий; сама запись — checkpoint пользователя (план 06-04).

Purpose: DOC-05 требует видео 1-3 мин. Готовый покадровый чеклист снимает риск, что пользователь забудет ключевой кадр (consent-диалог, живые счётчики, восстановление состояния).
Output: docs/demo-script.md (русский) с таблицей тактов и чеклистом записи.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-podacha/06-RESEARCH.md

<ready_content>
Готовый покадровый сценарий — в 06-RESEARCH.md раздел "DOC-05: сценарий видео-демо": таблица из 7 тактов (0:00-2:40) с колонками Такт/Время/Действие/Что видно, плюс "Чеклист записи" и "Замечание про фактическую готовность" (Android-live, iOS через TestFlight). Брать дословно, адаптировать под markdown-файл.
</ready_content>

<language_rule>
Файл на русском (stop-slop: активный залог, конкретика, без em-dash). Идентификаторы (Connect/Disconnect, rx/tx, getStatus, scrcpy, xcrun simctl) — английские.
</language_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Покадровый сценарий видео (7 тактов)</name>
  <files>docs/demo-script.md</files>
  <read_first>
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "DOC-05: сценарий видео-демо" — таблица тактов)
  </read_first>
  <action>
    Создать docs/demo-script.md с заголовком и таблицей из 7 тактов дословно из RESEARCH DOC-05: (1) запуск приложения на Android API 26+ — splash, Disconnected, ирис в покое, обе темы упомянуть; (2) опц. вставка vless:// из буфера — карточка сервера, UUID маскирован, tcping-задержка; (3) тап Connect -> системный consent-диалог prepare(); (4) переход Connecting -> Connected — ирис пульсирует/активен, значок VPN в статус-баре, таймер, FGS-уведомление; (5) живые данные — блок логов info/warning с цветами и копированием, счётчик rx (ping в подсеть туннеля), таймер; (6) тап Disconnect -> Disconnecting -> Disconnected — ирис гаснет, teardown в логах, статус-бар чист; (7) опц. перезапуск при активном VPN — getStatus() восстанавливает Connected. Указать целевую длительность 1-3 минуты.
  </action>
  <verify>
    <automated>test -f docs/demo-script.md && grep -q 'Connect' docs/demo-script.md && grep -q 'Disconnect' docs/demo-script.md && grep -qi 'consent' docs/demo-script.md && grep -qi 'getStatus' docs/demo-script.md && grep -qE '1-3|1–3' docs/demo-script.md && echo OK</automated>
    <human-check>Сценарий покрывает все 7 тактов; последовательность соответствует реальному поведению приложения</human-check>
  </verify>
  <acceptance_criteria>
    - docs/demo-script.md существует, таблица из 7 тактов с временными метками
    - Покрыты: запуск/Disconnected, (опц.) вставка vless, Connect+consent, Connecting->Connected+статусы, живые логи/трафик, Disconnect, (опц.) восстановление через getStatus
    - Указана длительность 1-3 минуты; текст на русском
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Чеклист записи + платформенная заметка</name>
  <files>docs/demo-script.md</files>
  <read_first>
    - docs/demo-script.md (после Task 1)
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "DOC-05 -> Чеклист записи" и "Замечание про фактическую готовность")
  </read_first>
  <action>
    Дописать в docs/demo-script.md секцию "Чеклист записи": ориентация под платформу, читаемый шрифт логов, БЕЗ реальных секретов в кадре (демо-конфиг фейковый echo.oko.vpn / 00000000-..., UUID маскирован в UI), длительность 1-3 мин; варианты инструмента записи (scrcpy для зеркала Android, QuickTime / xcrun simctl io recordVideo, встроенная запись устройства). Затем секция "Android vs iOS": базовое видео снять на Android (весь путь Connect->трафик->Disconnect живой); iOS-туннель реально исполняется только на устройстве через TestFlight (симулятор NE не хостит) — iOS показать статусами Swift-слоя либо отдельным TestFlight-клипом по возможности.
  </action>
  <verify>
    <automated>grep -qi 'чеклист' docs/demo-script.md && grep -qi 'секрет' docs/demo-script.md && grep -q 'TestFlight' docs/demo-script.md && grep -qiE 'scrcpy|simctl|QuickTime' docs/demo-script.md && echo OK</automated>
    <human-check>Чеклист явно запрещает реальные секреты в кадре; платформенная заметка честно разделяет Android-live и iOS-TestFlight</human-check>
  </verify>
  <acceptance_criteria>
    - Секция "Чеклист записи": ориентация, читаемость логов, запрет реальных секретов в кадре, длительность, инструменты записи
    - Секция "Android vs iOS": Android-live путь + iOS через TestFlight (ограничение симулятора)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| видео-кадр -> публичный зритель | Всё, что попадает в кадр демо, увидит ревьюер |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-06 | Information Disclosure | Кадр демо-видео | mitigate | Чеклист явно требует: без реальных vless/UUID в кадре (демо-конфиг фейковый), UUID маскирован в UI |
| T-6-SC | Tampering | pub/npm/pip installs | accept | Фаза документации не устанавливает пакетов |
</threat_model>

<verification>
- docs/demo-script.md покрывает 7 тактов + чеклист + платформенную заметку (grep-гейты)
- Ручная сверка сценария с реальным поведением приложения фаз 2-4
</verification>

<success_criteria>
- Покадровый сценарий 1-3 мин готов и точен относительно поведения приложения
- Чеклист записи запрещает реальные секреты в кадре
- Android-live и iOS-TestFlight честно разделены
</success_criteria>

<output>
Create `.planning/phases/06-podacha/06-03-SUMMARY.md` when done
</output>
