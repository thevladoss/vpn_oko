---
phase: 06-podacha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]
must_haves:
  truths:
    - "Ревьюер по README проходит путь от клона репозитория до flutter run на Android"
    - "mermaid-диаграмма рендерится на GitHub и показывает Flutter UI -> Bloc -> usecase -> repository -> Pigeon -> VpnService/PacketTunnelProvider"
    - "README называет, что взято open-source (версии) и что написано самостоятельно"
    - "iOS-раздел покрывает capabilities, entitlements, App Groups, app<->extension и ограничение симулятора"
    - "Раздел плана интеграции core называет точки startReadLoop/startTunnel, интерфейс VpnCore, .aar/.xcframework, gomobile/JNI"
    - "README честно перечисляет ограничения: нет core -> трафик не проксируется, tx=0, узкий маршрут 10.111.222.0/24, iOS только TestFlight, вставленный конфиг display-only"
  artifacts:
    - path: README.md
      provides: "Полный README на русском: шапка+бейдж, запуск, структура, архитектура+mermaid, iOS, план интеграции core, ограничения, что дальше"
      contains: "mermaid"
      min_lines: 120
  key_links:
    - from: README.md
      to: .github/workflows/ci.yml
      via: "CI badge URL path"
      pattern: "actions/workflows/ci.yml/badge.svg"
    - from: README.md
      to: android/app/src/main/kotlin/com/example/vpn_oko/vpn/OkoVpnService.kt
      via: "DOC-03 точка подключения core"
      pattern: "startReadLoop"
---

<objective>
Написать полный README.md на русском — единственный документ, по которому русскоязычный ревьюер за минуты клонирует проект, запускает его на Android и получает честную картину архитектуры, iOS-специфики, плана интеграции VPN-core и ограничений прототипа.

Purpose: README — лицо сдачи по ТЗ (GitHub + README + видео). Это вертикальный срез reviewer journey: запустить -> понять архитектуру и iOS -> понять план core и границы.
Output: README.md (русский, идентификаторы/mermaid/yaml — английский) с секциями Запуск, Структура, Архитектура+mermaid, Пять фаз, open-source vs своё, iOS, План интеграции VPN-core, Ограничения, Что дальше, CI-бейдж.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-podacha/06-RESEARCH.md
@.planning/phases/05-ios-network-extension/DOC-02-ios-setup.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<language_rule>
README и вся проза — на русском. По-английски только: идентификаторы кода, имена файлов/пакетов, версии, mermaid, yaml, команды, bundle id, названия API. Применять stop-slop (активный залог, без филлеров, конкретика, без em-dash, варьировать ритм). БЕЗ маркетинга — ревьюер оценивает инженерную честность.
</language_rule>

<ready_content>
Весь фактический материал уже готов в 06-RESEARCH.md — брать оттуда дословно, не изобретать:
- mermaid flowchart + sequenceDiagram: раздел "Architecture Patterns -> System Architecture Diagram"
- таблицы open-source / написано самостоятельно: раздел "Code Examples -> Таблица open-source vs своё"
- команды запуска и версии: раздел "Code Examples -> Команды запуска" (Flutter 3.44.5 / Dart 3.12.2, minSdk 26, targetSdk 36, JDK 17, Xcode 16+)
- iOS-материал: DOC-02-ios-setup.md (bundle id, entitlements, App Groups, ограничение симулятора) + раздел RESEARCH "Standard Stack -> iOS-параметры"
- DOC-03 материал: раздел "DOC-03: план интеграции VPN-core" (точки startReadLoop/startTunnel, эскиз интерфейса VpnCore, gomobile/JNI-таблица, варианты core)
- ограничения: раздел "Common Pitfalls -> Pitfall 4" + project_constraints (узкий маршрут, tx=0, iOS TestFlight, display-only конфиг)
</ready_content>

<code_facts>
Проверенные факты (не перепроверять, использовать в тексте):
- Точка core Android: `OkoVpnService.startReadLoop()` (android/app/src/main/kotlin/com/example/vpn_oko/vpn/OkoVpnService.kt, строка 79) читает TUN-fd и дропает пакеты, считает только rx; `establish()` строка 120; `addRoute("10.111.222.0", 24)` строка 117; `TrafficChangedMessage(rx.get(), 0L)` строка 105 (tx=0)
- Точка core iOS: `PacketTunnelProvider.startTunnel` (ios/PacketTunnel/PacketTunnelProvider.swift, строка 4) вызывает только `setTunnelNetworkSettings`, не читает `packetFlow`; `includedRoutes 10.111.222.0/24` строка 11
- Демо-конфиг фейковый: host `echo.oko.vpn`, uuid `00000000-0000-0000-0000-000000000000` (lib/app/di.dart строка 47) — использовать только его в примерах
- Pigeon-контракт закоммичен: pigeons/vpn_api.dart + lib/core/bridge/vpn_api.g.dart
- Бейдж CI (DOC-04): формат `.../actions/workflows/ci.yml/badge.svg` с плейсхолдером `<owner>/<repo>` (git remote не настроен; реальные owner/repo подставит пользователь на сдаче — план 06-04)
</code_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Шапка, бейдж, Запуск, Структура, Пять фаз, open-source vs своё (DOC-01, DOC-04-бейдж)</name>
  <files>README.md</files>
  <read_first>
    - README.md (сейчас дефолтная заглушка flutter create — заменяется целиком)
    - .planning/phases/06-podacha/06-RESEARCH.md (разделы: "Code Examples -> Команды запуска", "Code Examples -> Таблица open-source vs своё", "Recommended Project Structure", "DOC-04 -> Бейдж в README", "Standard Stack")
    - pubspec.yaml (версии пакетов для таблицы open-source)
  </read_first>
  <action>
    Полностью перезаписать README.md. Шапка: заголовок "Oko VPN — прототип нативного VPN на Flutter", одна строка бейджа CI по формату из RESEARCH "DOC-04 -> Бейдж" с плейсхолдером `<owner>/<repo>` и кликом на страницу прогонов (workflow-файл `ci.yml`, name `CI` — чтобы путь бейджа совпал с планом 06-02), затем 2-3 строки честного описания: прототип по тестовому заданию, реальный Android VpnService + типобезопасный мост Pigeon, iOS через TestFlight, без реального VPN-core (описан планом). Секция "Запуск": требования (Flutter 3.44.5 stable / Dart 3.12.2, JDK 17, Android SDK minSdk 26 / targetSdk 36, Xcode 16+ для iOS) и команды из RESEARCH дословно (flutter pub get; опциональная регенерация dart run pigeon --input pigeons/vpn_api.dart с пометкой "код уже закоммичен"; flutter run для Android API 26+; flutter build ipa + TestFlight для iOS; flutter analyze; flutter test). Секция "Структура проекта": дерево из RESEARCH "Recommended Project Structure" (lib/app, core/bridge, features/*, pigeons/, android/, ios/, test/, .github/workflows/ci.yml). Секция "Пять фаз": короткий список фаз 1-5 из ROADMAP (мост -> Android VpnService -> UI -> VLESS -> iOS NE) как путь сборки. Две таблицы "Использовано open-source" (pigeon ^27.1.1, flutter_bloc ^9.1.1, equatable ^2.1.0, google_fonts ^8.1.0, dev: very_good_analysis ^10.3.0, mocktail ^1.0.5, bloc_test ^10.0.0) и "Написано самостоятельно" из RESEARCH дословно. Без комментариев-заглушек, без маркетинга, активный залог.
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'actions/workflows/ci.yml/badge.svg' README.md && grep -q 'flutter pub get' README.md && grep -q 'flutter test' README.md && grep -q '3.44.5' README.md && grep -q 'minSdk 26' README.md && grep -qi 'open-source' README.md && grep -q 'pigeon' README.md && echo OK</automated>
    <human-check>README читается как честный русский текст без филлеров; бейдж-строка корректна; таблицы open-source/своё полные</human-check>
  </verify>
  <acceptance_criteria>
    - README.md перезаписан (дефолтная заглушка "A new Flutter project" удалена)
    - Присутствует строка CI-бейджа с путём actions/workflows/ci.yml/badge.svg и плейсхолдером <owner>/<repo>
    - Секция "Запуск" содержит версии (3.44.5, minSdk 26, targetSdk 36) и команды flutter pub get / run / analyze / test
    - Секция "Структура" содержит дерево с lib/features, pigeons/, .github/workflows/ci.yml
    - Две таблицы: open-source (с версиями) и написано самостоятельно
    - Текст на русском, идентификаторы/версии/команды на английском, без комментариев-заглушек
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Архитектура + mermaid + iOS-раздел (DOC-01 архитектура, DOC-02)</name>
  <files>README.md</files>
  <read_first>
    - README.md (после Task 1)
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "Architecture Patterns -> System Architecture Diagram": готовые mermaid flowchart и sequenceDiagram; "Component Responsibilities")
    - .planning/phases/05-ios-network-extension/DOC-02-ios-setup.md (весь iOS-материал)
    - .planning/codebase/ARCHITECTURE.md (слои, ключевые правила)
  </read_first>
  <action>
    Добавить в README секцию "Архитектура". Вставить mermaid flowchart из RESEARCH "System Architecture Diagram" дословно (fenced-блок ```mermaid, метки со спецсимволами и `<br/>` в кавычках `["..."]`, стрелки `-->` сплошные и `-.->` пунктирные для событий, ID узлов латиницей) — путь Flutter UI -> Bloc -> usecase -> repository -> VpnBridge -> Pigeon -> Android OkoVpnService / iOS PacketTunnelProvider. Затем sequenceDiagram потока Connect из RESEARCH дословно. Под диаграммами — таблица маппинга слой->файлы из RESEARCH "Component Responsibilities" (presentation/domain/data/core-bridge/Android native/iOS native). Далее секция "iOS: Network Extension" из DOC-02-ios-setup.md: bundle id (com.example.vpnOko, .PacketTunnel), entitlements (networking.networkextension packet-tunnel-provider, application-groups group.com.example.vpnOko), App Groups как канал app<->extension, схема Runner.app <-> PacketTunnel.appex, и явное ограничение симулятора (iOS Simulator не хостит packet-tunnel NE, честный путь ошибки connecting->error, реальная проверка через TestFlight на устройстве). Не добавлять signing-секреты сверх уже присутствующих в проекте (Team ID Z2GDTXHVZZ уже в pbxproj — упоминать не требуется).
  </action>
  <verify>
    <automated>grep -c '```mermaid' README.md | grep -qE '^[2-9]' && grep -q 'flowchart' README.md && grep -q 'sequenceDiagram' README.md && grep -q 'group.com.example.vpnOko' README.md && grep -q 'packet-tunnel-provider' README.md && grep -qi 'симулятор' README.md && echo OK</automated>
    <human-check>Обе mermaid-диаграммы рендерятся в GitHub preview (или mermaid.live), не показываются как сырой код; iOS-раздел точен относительно entitlements/App Groups</human-check>
  </verify>
  <acceptance_criteria>
    - Два блока ```mermaid: flowchart (Flutter->Pigeon->VpnService/NE) и sequenceDiagram (поток Connect)
    - Метки диаграмм со спецсимволами в кавычках, стрелки --> и -.->; диаграммы валидны для нативного рендера GitHub
    - Таблица маппинга слой->файлы присутствует
    - iOS-раздел покрывает capabilities, entitlements, App Groups (group.com.example.vpnOko), app<->extension, ограничение симулятора и TestFlight
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: План интеграции VPN-core + Ограничения + Что дальше (DOC-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md (после Task 2)
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "DOC-03: план интеграции VPN-core" целиком; "Common Pitfalls -> Pitfall 4"; project_constraints)
    - android/app/src/main/kotlin/com/example/vpn_oko/vpn/OkoVpnService.kt (строки 79-120: startReadLoop, establish, addRoute)
    - ios/PacketTunnel/PacketTunnelProvider.swift (строки 4-20: startTunnel, setTunnelNetworkSettings)
  </read_first>
  <action>
    Добавить секцию "План интеграции VPN-core" из RESEARCH DOC-03. Назвать точную точку подключения в коде: Android `OkoVpnService.startReadLoop()` (сейчас читает TUN-fd от `establish()` и дропает пакеты, считает rx, tx=0) — сюда встаёт core, дескриптор передаётся в libbox/sing-box, `VpnService.protect(socket)` защищает исходящий сокет; iOS `PacketTunnelProvider.startTunnel` (сейчас только `setTunnelNetworkSettings`, `packetFlow` не читается) — сюда встаёт тот же core через `packetFlow.readPackets`/`writePackets`. Описать предлагаемый интерфейс VpnCore СЛОВАМИ и коротким эскизом сигнатур (Kotlin interface с start(tunFd, config, protect)/stop()/stats() и Swift protocol с start(packetFlow, config)/stop()) с явной пометкой "в коде пока нет — это план; реальный интерфейс не вводится, чтобы не плодить мёртвый код (ловит very_good_analysis)". Подчеркнуть: Pigeon-контракт НЕ меняется, фасад заложен под core. Таблица механики gomobile/JNI из RESEARCH (Android libbox.aar через gomobile bind -target=android, in-process JNI; iOS Libbox.xcframework через gomobile bind -target=ios; Dart FFI НЕ место интеграции — core в нативном VPN-процессе). Варианты core: sing-box/libbox (первичный), Xray/libXray, libv2ray (исторический), с пометкой ASSUMED на конкретных символах API. Затем секция "Ограничения" честным списком: нет реального core -> трафик не проксируется; tx всегда 0 (read-loop считает только rx); маршрут узкий 10.111.222.0/24 (интернет устройства жив в Connected, счётчики через ping в подсеть); iOS-туннель реально проверяется только на устройстве через TestFlight; вставленный vless-конфиг display-only, в реальный Connect не проводится (VpnConnectionBloc.config = demoConfig). Секция "Что бы делал дальше": реальный core, iOS traffic read-loop, kill switch/split tunneling как направления (per ТЗ и v2 requirements). Формулировки честные, без обещаний сверх реализованного.
  </action>
  <verify>
    <automated>grep -q 'startReadLoop' README.md && grep -q 'startTunnel' README.md && grep -q 'VpnCore' README.md && grep -q 'gomobile' README.md && grep -q '10.111.222.0/24' README.md && grep -qi 'ограничени' README.md && grep -q 'TestFlight' README.md && echo OK</automated>
    <human-check>DOC-03 честно называет точки подключения и интерфейс словами; раздел ограничений не обещает больше, чем реализовано (нет "полноценный VPN"/"шифрует трафик")</human-check>
  </verify>
  <acceptance_criteria>
    - Секция плана core называет startReadLoop (Android) и startTunnel (iOS) как точки подключения
    - Интерфейс VpnCore описан словами + эскиз сигнатур, с пометкой "в коде нет, план"
    - Присутствует таблица механики: .aar/.xcframework, gomobile bind, JNI, "Dart FFI не место интеграции"
    - Названы варианты core (sing-box первичный, Xray/libXray, libv2ray)
    - Секция "Ограничения" перечисляет: нет core/трафик не проксируется, tx=0, узкий маршрут 10.111.222.0/24, iOS TestFlight, конфиг display-only
    - Секция "Что дальше" присутствует
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content -> публичный ревьюер | Всё, что попадает в README и репозиторий, читает внешний ревьюер |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Information Disclosure | Примеры конфигов в README | mitigate | Использовать только фейковый демо-конфиг (host echo.oko.vpn, uuid 00000000-0000-0000-0000-000000000000 из lib/app/di.dart); реальные vless://-ссылки и UUID в README запрещены |
| T-6-05 | Information Disclosure | iOS-раздел README | mitigate | Не добавлять signing-секреты (ключи, профили) сверх уже присутствующего в проекте; описывать только публичные bundle id / entitlement-строки |
| T-6-04-SC | Tampering | pub/npm/pip installs | accept | Фаза README не устанавливает пакетов; версии в таблице берутся из закоммиченного pubspec.yaml |
</threat_model>

<verification>
- `flutter analyze` не затрагивается (README не код), но структура README проверяется grep-гейтами задач
- Обе mermaid-диаграммы рендерятся в GitHub preview / mermaid.live (human-check)
- Ручная вычитка русского текста на честность и stop-slop
- Ключевой линк: путь CI-бейджа совпадает с workflow-файлом плана 06-02 (ci.yml, name CI)
</verification>

<success_criteria>
- README.md ведёт от клона до flutter run на Android; версии и команды корректны
- mermaid flowchart + sequenceDiagram валидны и рендерятся на GitHub
- Указано open-source (версии) и написано самостоятельно
- iOS-раздел покрывает capabilities/entitlements/App Groups/app<->extension/симулятор (DOC-02)
- План интеграции core называет точки startReadLoop/startTunnel, интерфейс VpnCore, .aar/.xcframework, gomobile/JNI (DOC-03)
- Ограничения перечислены честно; никаких обещаний сверх реализованного
</success_criteria>

<output>
Create `.planning/phases/06-podacha/06-01-SUMMARY.md` when done
</output>
