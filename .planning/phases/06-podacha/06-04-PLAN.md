---
phase: 06-podacha
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified: []
autonomous: false
requirements: [DOC-04, DOC-05]
must_haves:
  truths:
    - "Проект живёт в публичном GitHub-репозитории; первый прогон CI зелёный, бейдж активен"
    - "Демо-видео 1-3 минуты записано по сценарию и доступно ревьюеру"
    - "В публичный репозиторий не попали ТЗ-docx и реальные секреты"
  artifacts: []
  key_links:
    - from: README.md
      to: публичный GitHub-репозиторий
      via: "owner/repo в бейдже подставлены реальными значениями после создания репо"
      pattern: "actions/workflows/ci.yml/badge.svg"
---

<objective>
Довести проект до сдаваемого состояния: пользователь создаёт публичный репозиторий, пушит код, дожидается зелёного первого прогона CI и активного бейджа, затем записывает демо-видео по docs/demo-script.md и добавляет ссылку в README. Это финальный вертикальный срез: ревьюер получает рабочую ссылку на репозиторий с зелёным CI и видео.

Purpose: git remote не настроен; создание публичного репо и запись видео — единственные шаги, которые Claude не может выполнить (нет CLI-доступа к аккаунту пользователя и к записи экрана устройства).
Output: публичный репозиторий с зелёным CI + бейджем, демо-видео 1-3 мин, ссылка на видео в README.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-podacha/06-RESEARCH.md
@README.md
@docs/demo-script.md

<why_checkpoints>
06-RESEARCH.md "Environment Availability": git remote не настроен (git remote -v пуст) — обязательный шаг пользователя без фолбэка. Инструмент записи экрана — на выбор пользователя. Эти два шага вынесены в checkpoint честно (VALIDATION.md "Manual-Only Verifications").
</why_checkpoints>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Публичный репозиторий + push + зелёный CI + бейдж (DOC-04)</name>
  <what-built>
    README.md (план 06-01), .github/workflows/ci.yml (план 06-02) и docs/demo-script.md (план 06-03) готовы локально. CI-последовательность flutter analyze + flutter test прошла локально зелёной. Git remote не настроен — публичный репозиторий и push выполняет пользователь.
  </what-built>
  <how-to-verify>
    1. Перед пушем убедиться, что приватное не попадёт в публичный репо: `git ls-files | grep -i docx` должно быть пусто (ТЗ_*.docx в .gitignore строка 48); `git ls-files` не содержит ключей/профилей/реальных vless-ссылок.
    2. Создать ПУБЛИЧНЫЙ репозиторий на GitHub (например `<owner>/vpn_oko`).
    3. Подключить remote и запушить main: `git remote add origin <url>` и `git push -u origin main`.
    4. Открыть вкладку Actions репозитория, дождаться первого прогона workflow "CI" — он должен завершиться зелёным (analyze + test).
    5. В README.md заменить плейсхолдеры `<owner>/<repo>` в строке бейджа на реальные owner/repo, закоммитить и запушить; убедиться, что бейдж в шапке README стал зелёным (не серый "no status"), клик ведёт на страницу прогонов.
  </how-to-verify>
  <resume-signal>Напишите "approved", когда публичный репо создан, CI зелёный и бейдж активен — или опишите, что пошло не так (красный CI, серый бейдж)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Запись демо-видео 1-3 минуты (DOC-05)</name>
  <what-built>
    docs/demo-script.md (план 06-03) содержит покадровый сценарий из 7 тактов и чеклист записи. Приложение фаз 1-5 готово: Android-путь Connect -> статусы/логи/трафик -> Disconnect живой; iOS — через TestFlight на устройстве.
  </what-built>
  <how-to-verify>
    1. Открыть docs/demo-script.md и записать видео 1-3 минуты, следуя 7 тактам: запуск/Disconnected -> (опц.) вставка vless -> Connect + системный consent -> Connecting->Connected (статусы, значок VPN, таймер, FGS-уведомление) -> живые логи и счётчик rx -> Disconnect -> (опц.) восстановление через getStatus.
    2. Проверить по чеклисту: без реальных секретов в кадре (демо-конфиг фейковый, UUID маскирован), читаемый шрифт логов, длительность 1-3 мин.
    3. Базовое видео снять на Android (весь путь живой); iOS по возможности — отдельным TestFlight-клипом или статусами Swift-слоя.
    4. Загрузить видео и добавить ссылку в README (секция "Демо"); закоммитить и запушить.
  </how-to-verify>
  <resume-signal>Напишите "approved", когда видео записано, проверено по чеклисту и ссылка добавлена в README — или опишите проблему</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| локальный репозиторий -> публичный GitHub | Всё запушенное становится публичным |
| демо-видео -> публичный зритель | Кадры видео видит ревьюер |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Information Disclosure | Коммит в публичный репозиторий | mitigate | Перед push проверить `git ls-files` — нет ТЗ_*.docx (в .gitignore строка 48), нет ключей/профилей/реальных vless-ссылок; blocking-checkpoint не авто-аппрувится |
| T-6-07 | Information Disclosure | Кадр демо-видео | mitigate | Запись без реальных vless/UUID в кадре (демо-конфиг фейковый, UUID маскирован) — по чеклисту docs/demo-script.md |
</threat_model>

<verification>
- `git ls-files | grep -i docx` пусто до пуша (ручная проверка в checkpoint)
- Первый прогон workflow "CI" зелёный на GitHub Actions
- Бейдж в README активен (зелёный), owner/repo реальные
- Видео 1-3 мин доступно по ссылке в README, снято по чеклисту
</verification>

<success_criteria>
- Публичный репозиторий создан, main запушен, приватное (ТЗ-docx, секреты) не попало
- Первый CI-прогон зелёный, бейдж активен и кликабелен
- Демо-видео 1-3 мин записано по сценарию и доступно ревьюеру
</success_criteria>

<output>
Create `.planning/phases/06-podacha/06-04-SUMMARY.md` when done
</output>
