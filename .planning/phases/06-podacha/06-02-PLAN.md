---
phase: 06-podacha
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/ci.yml, test/flutter_test_config.dart]
autonomous: true
requirements: [DOC-04]
must_haves:
  truths:
    - "CI-workflow гоняет flutter analyze + flutter test на ubuntu-latest без сборки APK"
    - "Точная CI-последовательность зелёная локально на чистом чекауте (риск google_fonts снят до пуша)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "GitHub Actions workflow analyze+test, name CI, subosito/flutter-action@v2, Flutter 3.44.5"
      contains: "flutter test"
  key_links:
    - from: .github/workflows/ci.yml
      to: README.md
      via: "workflow name CI + файл ci.yml совпадают с путём бейджа в README (план 06-01)"
      pattern: "name: CI"
---

<objective>
Создать GitHub Actions workflow, который на каждый push/PR в main прогоняет `flutter analyze` + `flutter test` (без сборки APK/IPA) и служит self-validating доказательством качества для ревьюера. Wave 0 фазы: прогнать точную CI-последовательность локально на чистом чекауте и снять риск google_fonts-флейка ДО пуша.

Purpose: зелёный CI-бейдж — критерий DOC-04 и сигнал инженерной дисциплины. Workflow должен пройти с первого прогона после пуша (план 06-04).
Output: .github/workflows/ci.yml (валидный, actions запинены тегами) + подтверждённо зелёная локальная последовательность analyze+test.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-podacha/06-RESEARCH.md

<ready_content>
Готовый workflow — в 06-RESEARCH.md раздел "DOC-04: CI -> Готовый workflow". Брать дословно. Ключевые решения из RESEARCH:
- runs-on ubuntu-latest (не macOS — не собираем iOS/APK)
- actions/checkout@v4 + subosito/flutter-action@v2 (channel stable, flutter-version 3.44.5)
- steps: flutter pub get -> flutter analyze -> flutter test; НЕТ flutter build (Anti-Patterns: CI со сборкой APK/IPA)
- actions/setup-java НЕ нужен (analyze+test без Gradle-сборки)
- кодоген Pigeon НЕ запускается (vpn_api.g.dart закоммичен)
- пин тегов actions (не @master/@main) — Package Legitimacy Audit: subosito/flutter-action и actions/checkout approved, официальные/де-факто-стандарт
</ready_content>

<flake_risk>
06-RESEARCH.md "Common Pitfalls -> Pitfall 1" + "DOC-04 -> Риск зелёного прогона": виджет-тесты используют google_fonts. Шрифты забандлены офлайн (google_fonts/*.ttf, matching по имени файла), гварды allowRuntimeFetching в тестах нет. Обычно бандл находится и тесты проходят, но headless CI без сети — источник флейка. Фикс при флейке: создать test/flutter_test_config.dart с GoogleFonts.config.allowRuntimeFetching = false. Файл создаётся ТОЛЬКО если локальный прогон офлайн покажет флейк; test-инфраструктура, не код приложения.
</flake_risk>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать .github/workflows/ci.yml (DOC-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "DOC-04: CI -> Готовый workflow" и "Package Legitimacy Audit")
    - pubspec.yaml (подтвердить, что pub get подтянет зафиксированный pubspec.lock)
  </read_first>
  <action>
    Создать .github/workflows/ci.yml по образцу из RESEARCH "DOC-04 -> Готовый workflow" дословно. name: CI (совпадает с путём бейджа в README плана 06-01). Триггеры on push и pull_request в branches [main]. Job analyze-and-test на runs-on ubuntu-latest. Steps: uses actions/checkout@v4; uses subosito/flutter-action@v2 с with channel stable и flutter-version 3.44.5; run flutter pub get; run flutter analyze; run flutter test. Без flutter build, без setup-java, без кодогена pigeon. Пин тегов @v4 и @v2 (не @master/@main). БЕЗ пояснительных yaml-комментариев (по духу правила "без комментариев в коде"); служебные ключи yaml допустимы.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'name: CI' .github/workflows/ci.yml && grep -q 'subosito/flutter-action@v2' .github/workflows/ci.yml && grep -q 'actions/checkout@v4' .github/workflows/ci.yml && grep -q '3.44.5' .github/workflows/ci.yml && grep -q 'flutter analyze' .github/workflows/ci.yml && grep -q 'flutter test' .github/workflows/ci.yml && ! grep -q 'flutter build' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null && echo OK</automated>
    <human-check>yaml валиден (если python-yaml недоступен — визуально проверить отступы); actions запинены тегами</human-check>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml существует, name: CI
    - Триггеры push + pull_request в main; runs-on ubuntu-latest
    - actions/checkout@v4 и subosito/flutter-action@v2 (channel stable, flutter-version 3.44.5), запинены тегами
    - Steps: flutter pub get, flutter analyze, flutter test; НЕТ flutter build / setup-java / pigeon-кодогена
    - Нет пояснительных yaml-комментариев
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Self-validating прогон analyze+test локально, снять риск google_fonts (Wave 0)</name>
  <files>test/flutter_test_config.dart</files>
  <read_first>
    - .github/workflows/ci.yml (после Task 1 — воспроизвести те же шаги локально)
    - .planning/phases/06-podacha/06-RESEARCH.md (раздел "Common Pitfalls -> Pitfall 1"; "Validation Architecture")
    - analysis_options.yaml (very_good_analysis; исключения *.g.dart, pigeons/**)
  </read_first>
  <action>
    Прогнать точную CI-последовательность локально на чистом чекауте: flutter pub get, затем flutter analyze, затем flutter test. Если возможно — прогнать flutter test с отключённой сетью (эмуляция headless CI), чтобы поймать google_fonts-флейк заранее. Если все три команды зелёные без сети — файл test/flutter_test_config.dart НЕ создавать (не плодить лишнее). Если flutter test флейкает на google_fonts (сетевой fetch шрифта в headless) — создать test/flutter_test_config.dart с асинхронной testExecutable-обёрткой, выставляющей GoogleFonts.config.allowRuntimeFetching = false перед прогоном (форсирует офлайн-бандл), и перепрогнать до зелёного. Это test-инфраструктура, код приложения не трогать. analyze должен быть чистым (very_good_analysis падает на любом info/warning; генерённые файлы уже исключены).
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze && flutter test</automated>
    <human-check>Все три команды зелёные; если сеть отключалась — тесты прошли офлайн; решение по flutter_test_config.dart зафиксировано в SUMMARY (создан/не понадобился)</human-check>
  </verify>
  <acceptance_criteria>
    - flutter pub get, flutter analyze, flutter test — все зелёные локально
    - Риск google_fonts проверен (по возможности офлайн-прогоном)
    - test/flutter_test_config.dart создан ТОЛЬКО при подтверждённом флейке (иначе отсутствует); решение задокументировано в SUMMARY
    - Код приложения (lib/, android/, ios/) не изменён
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> публичные логи | Логи прогона CI публичны в GitHub Actions |
| GitHub Actions зависимости -> раннер | Внешние actions исполняются на раннере с доступом к репозиторию |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-02 | Information Disclosure | CI job logs | mitigate | Workflow без сборки/подписи -> секреты и env не используются; никаких `secrets.*` в ci.yml |
| T-6-04 | Tampering | GitHub Actions зависимости | mitigate | Пин тегов actions/checkout@v4 и subosito/flutter-action@v2 (не @master/@main); оба verified в RESEARCH Package Legitimacy Audit как официальные/де-факто-стандарт |
| T-6-SC | Tampering | pub/npm/pip installs | accept | Фаза не устанавливает пакетов; `flutter pub get` подтягивает зафиксированный pubspec.lock; supply-chain checkpoint неприменим (нет [ASSUMED]/[SUS] пакетов) |
</threat_model>

<verification>
- ci.yml валиден (python yaml.safe_load или визуально), grep подтверждает structure и отсутствие flutter build
- `flutter pub get && flutter analyze && flutter test` зелёные локально (та же последовательность, что в CI)
- Ключевой линк: name CI + файл ci.yml совпадают с путём бейджа README (план 06-01)
</verification>

<success_criteria>
- .github/workflows/ci.yml гоняет analyze+test на ubuntu-latest без APK-сборки
- Локальная CI-последовательность зелёная; риск google_fonts снят
- Actions запинены тегами; секреты не используются
</success_criteria>

<output>
Create `.planning/phases/06-podacha/06-02-SUMMARY.md` when done
</output>
