---
phase: 01-pigeon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - pigeons/vpn_api.dart
  - lib/core/bridge/vpn_api.g.dart
  - android/app/src/main/kotlin/com/example/vpn_oko/bridge/Messages.g.kt
  - ios/Runner/Bridge/Messages.g.swift
autonomous: true
requirements: [BRG-01, BRG-02]
must_haves:
  truths:
    - "dart run pigeon генерирует Dart, Kotlin и Swift без ошибок"
    - "Контракт объявляет startVpn/stopVpn/getStatus (@HostApi) и vpnEvents() (@EventChannelApi) поверх sealed VpnEventMessage"
    - "Сгенерированные символы моста видны в выходных файлах и сверены с research"
  artifacts:
    - path: "pigeons/vpn_api.dart"
      provides: "VpnHostApi, VpnEventsApi, VpnConfigMessage, VpnStatusSnapshotMessage, sealed VpnEventMessage, enum VpnStatusMessage"
      contains: "@EventChannelApi"
    - path: "lib/core/bridge/vpn_api.g.dart"
      provides: "Dart-клиент VpnHostApi + top-level vpnEvents(): Stream<VpnEventMessage>"
      contains: "vpnEvents"
    - path: "android/app/src/main/kotlin/com/example/vpn_oko/bridge/Messages.g.kt"
      provides: "Kotlin VpnHostApi.setUp + VpnEventsStreamHandler + PigeonEventSink"
      contains: "VpnEventsStreamHandler"
    - path: "ios/Runner/Bridge/Messages.g.swift"
      provides: "Swift VpnHostApiSetup.setUp + VpnEventsStreamHandler + PigeonEventSink"
      contains: "VpnEventsStreamHandler"
  key_links:
    - from: "pubspec.yaml"
      to: "pigeon 27.1.1"
      via: "dev_dependencies"
      pattern: "pigeon"
    - from: "pigeons/vpn_api.dart"
      to: "outputs (dartOut/kotlinOut/swiftOut)"
      via: "@ConfigurePigeon"
      pattern: "ConfigurePigeon"
---

<objective>
Зафиксировать фундамент фазы: зависимости стека, строгий линт и типобезопасный контракт моста с кодогеном на три языка. Это контракт-first шаг walking-skeleton — все остальные планы фазы зависят от сгенерированных символов.

Purpose: снять главную неопределённость проекта (генерация StreamHandler в pigeon 27.x) и точные имена сгенерированных символов (Open Question 1, assumptions A2/A4) на самой дешёвой задаче, до нативного и Dart-кода.
Output: pubspec с утверждённым стеком, `analysis_options.yaml` на very_good_analysis, `pigeons/vpn_api.dart`, три сгенерированных файла моста.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-pigeon/01-RESEARCH.md
@.planning/phases/01-pigeon/SKELETON.md

<interfaces>
<!-- Целевой контракт из RESEARCH Code Example 1. Поля non-null с required-конструктором; при ошибке codegen перейти на late-поля (assumption A1). -->

pigeons/vpn_api.dart объявляет:
- enum VpnStatusMessage { disconnected, connecting, connected, disconnecting, error }
- class VpnConfigMessage { String host; int port; String userId; String serverName; }   // НЕ расширять под VLESS — Phase 4
- class VpnStatusSnapshotMessage { VpnStatusMessage status; int? connectedSinceEpochMs; int rxBytes; int txBytes; }
- sealed class VpnEventMessage {}
- class StatusChangedMessage extends VpnEventMessage { VpnStatusMessage status; int? connectedSinceEpochMs; }
- class LogMessage extends VpnEventMessage { String text; int timestampMillis; String level; }
- class TrafficChangedMessage extends VpnEventMessage { int rxBytes; int txBytes; }
- class ErrorMessage extends VpnEventMessage { String code; String description; }
- @HostApi abstract VpnHostApi { @async void startVpn(VpnConfigMessage); @async void stopVpn(); VpnStatusSnapshotMessage getStatus(); }
- @EventChannelApi abstract VpnEventsApi { VpnEventMessage vpnEvents(); }

@ConfigurePigeon outputs:
- dartOut:  lib/core/bridge/vpn_api.g.dart
- kotlinOut: android/app/src/main/kotlin/com/example/vpn_oko/bridge/Messages.g.kt  (package com.example.vpn_oko.bridge)
- swiftOut: ios/Runner/Bridge/Messages.g.swift
- dartPackageName: vpn_oko
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → проект | пакеты зависимостей попадают в сборку |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | pub add pigeon/flutter_bloc/equatable/mocktail/very_good_analysis | mitigate | Только pub.dev-пакеты; издатели верифицированы в RESEARCH «Package Legitimacy Audit» (flutter.dev, felangel, Very Good Ventures — все Approved); версии зафиксированы точными числами; slopcheck неприменим (не npm/PyPI). Блокирующий checkpoint не нужен: нет `[ASSUMED]`/`[SUS]` пакетов |
| T-1-01 | Information Disclosure | контракт LogMessage | mitigate (проактивно) | `LogMessage` в контракте — свободный текст; в реализации echo и позже не класть UUID/поля VpnConfig; маскировка VLESS-кред — Phase 4 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Зависимости стека и строгий линт</name>
  <files>pubspec.yaml, analysis_options.yaml</files>
  <read_first>
    - pubspec.yaml (текущий: только flutter_lints)
    - analysis_options.yaml (текущий: include flutter_lints)
    - .planning/phases/01-pigeon/01-RESEARCH.md § «Standard Stack» + «Installation» (точные версии и содержимое analysis_options)
  </read_first>
  <action>
    Добавить в pubspec.yaml зависимости точными версиями из RESEARCH: runtime — flutter_bloc 9.1.1, equatable 2.1.0; dev — pigeon 27.1.1, mocktail 1.0.5, very_good_analysis 10.3.0. Ставить через `flutter pub add flutter_bloc equatable` и `flutter pub add --dev pigeon mocktail very_good_analysis`; при расхождении версий закрепить их вручную. Заменить `analysis_options.yaml`: `include: package:very_good_analysis/analysis_options.yaml`; в `linter.rules` выставить `public_member_api_docs: false` (конфликтует с правилом «без комментариев»); в `analyzer.exclude` добавить `"**/*.g.dart"` (генерированный код не проходит строгий VGA-линт и руками не редактируется). flutter_bloc и equatable здесь только объявляются — Bloc подключается в Phase 3, equatable используют доменные модели Plan 02.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` завершается без ошибок; в `pubspec.lock` есть pigeon 27.1.1, flutter_bloc 9.1.1, equatable 2.1.0, mocktail 1.0.5, very_good_analysis 10.3.0
    - `analysis_options.yaml` включает very_good_analysis, содержит `public_member_api_docs: false` и `exclude: "**/*.g.dart"`
    - `flutter analyze` не падает на дефолтном шаблоне (main.dart «Hello World» ещё может дать warnings — критичны только errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Контракт pigeon и кодоген трёх языков</name>
  <files>pigeons/vpn_api.dart, lib/core/bridge/vpn_api.g.dart, android/app/src/main/kotlin/com/example/vpn_oko/bridge/Messages.g.kt, ios/Runner/Bridge/Messages.g.swift</files>
  <read_first>
    - .planning/phases/01-pigeon/01-RESEARCH.md § «Code Example 1» (полный контракт) + «Open Question 1» + «Assumptions Log» A1/A2/A4
    - .planning/research/ARCHITECTURE.md § «Pattern 1» (суффикс Message, снапшот в getStatus)
  </read_first>
  <action>
    Создать `pigeons/vpn_api.dart` по Code Example 1: заголовок `@ConfigurePigeon(PigeonOptions(...))` с dartOut/kotlinOut (package `com.example.vpn_oko.bridge`)/swiftOut/dartPackageName из блока `<interfaces>`; enum `VpnStatusMessage`; DTO `VpnConfigMessage` (host/port/userId/serverName — НЕ добавлять VLESS-поля), `VpnStatusSnapshotMessage` (status/connectedSinceEpochMs/rxBytes/txBytes); `sealed class VpnEventMessage` с подклассами `StatusChangedMessage`, `LogMessage`, `TrafficChangedMessage`, `ErrorMessage`; `@HostApi VpnHostApi` со `@async startVpn/stopVpn` и `getStatus()` → `VpnStatusSnapshotMessage`; `@EventChannelApi VpnEventsApi { VpnEventMessage vpnEvents(); }`. Только kotlinOut+swiftOut+dartOut — javaOut/objcOut не указывать (валят event channels, Pitfall 3). Сгенерировать `dart run pigeon --input pigeons/vpn_api.dart`. Если codegen отвергнет non-null поля (A1) — перевести data-поля на `late`. После генерации свериться с выходными файлами и снять точные символы (A2/A4): Kotlin `VpnHostApi.setUp`, `VpnEventsStreamHandler`, `PigeonEventSink`, регистр enum-констант; Swift `VpnHostApiSetup.setUp`, `VpnEventsStreamHandler.register(with:streamHandler:)`; Dart top-level `vpnEvents()`. Зафиксировать наблюдённые имена в SUMMARY для планов 03/05/06.
  </action>
  <verify>
    <automated>dart run pigeon --input pigeons/vpn_api.dart && flutter analyze lib/core/bridge/vpn_api.g.dart</automated>
  </verify>
  <acceptance_criteria>
    - Генерация проходит без ошибок; созданы три файла: `lib/core/bridge/vpn_api.g.dart`, `android/.../bridge/Messages.g.kt`, `ios/Runner/Bridge/Messages.g.swift`
    - `grep -v '^//' lib/core/bridge/vpn_api.g.dart | grep -c 'vpnEvents'` больше 0 (Dart-стрим сгенерирован)
    - `grep -c 'VpnEventsStreamHandler' android/app/src/main/kotlin/com/example/vpn_oko/bridge/Messages.g.kt` больше 0 и то же для `ios/Runner/Bridge/Messages.g.swift` (StreamHandler сгенерирован для обеих платформ — гипотеза STATE.md подтверждена, @FlutterApi-fallback не нужен)
    - Точные имена символов (setUp-суффиксы, регистр enum-констант Kotlin) записаны в SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `flutter pub get` и `flutter analyze` без ошибок
- `dart run pigeon --input pigeons/vpn_api.dart` идемпотентен и без ошибок
- Три сгенерированных файла существуют; StreamHandler присутствует в Kotlin и Swift выходах
</verification>

<success_criteria>
Контракт моста и стек зафиксированы; кодоген трёх языков подтверждён; точные символы сняты и переданы вниз по волнам. Планы 03/05/06 могут писать реализации без разведки имён.
</success_criteria>

<output>
Create `.planning/phases/01-pigeon/01-01-SUMMARY.md` when done. Обязательно занести в SUMMARY наблюдённые имена сгенерированных символов (Kotlin/Swift setUp, регистр enum, точное имя Dart-стрима) — это вход для планов 03, 05, 06.
</output>
