---
phase: 01-pigeon
plan: 06
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - ios/Runner/AppDelegate.swift
  - ios/Runner/Bridge/VpnHostApiImpl.swift
  - ios/Runner/Bridge/VpnEventListener.swift
  - ios/Runner.xcodeproj/project.pbxproj
autonomous: true
requirements: [BRG-01, BRG-02, BRG-04]
must_haves:
  truths:
    - "AppDelegate регистрирует VpnHostApi и VpnEventsStreamHandler в didInitializeImplicitFlutterEngine через applicationRegistrar.messenger()"
    - "echo startVpn эмитит синтетическую цепочку LogMessage → StatusChanged(connecting) → StatusChanged(connected)"
    - "echo LogMessage выставляет level строковым литералом нижнего регистра (\"info\"), совпадающим с именами Dart-enum LogLevel { info, warning, error } — контракт с маппером 01-03"
    - "события уходят в sink только с DispatchQueue.main; последний статус реплеится в onListen; getStatus отдаёт снапшот"
    - "новые Swift-файлы (Messages.g.swift, Bridge/*) входят в build sources таргета Runner"
  artifacts:
    - path: "ios/Runner/AppDelegate.swift"
      provides: "регистрация HostApi + StreamHandler на шаблоне Flutter 3.44"
      contains: "didInitializeImplicitFlutterEngine"
    - path: "ios/Runner/Bridge/VpnEventListener.swift"
      provides: "VpnEventsStreamHandler impl: main-queue доставка + replay lastStatus"
      contains: "DispatchQueue.main"
    - path: "ios/Runner/Bridge/VpnHostApiImpl.swift"
      provides: "echo startVpn/stopVpn/getStatus"
      contains: "getStatus"
  key_links:
    - from: "AppDelegate"
      to: "messenger"
      via: "engineBridge.applicationRegistrar.messenger()"
      pattern: "applicationRegistrar"
    - from: "VpnEventListener.onListen"
      to: "replay lastStatus"
      via: "emit(lastStatus) в onListen"
      pattern: "lastStatus"
    - from: "VpnHostApiImpl echo LogMessage.level"
      to: "маппер 01-03 LogLevel.values.byName"
      via: "строковый литерал нижнего регистра \"info\""
      pattern: "\"info\""
---

<objective>
Реализовать iOS-сторону echo-моста в Runner-приложении: регистрацию pigeon-реализаций по новому шаблону Flutter 3.44 (SceneDelegate) и синтетический эмиттер с доставкой строго с main queue. Network Extension и реальный туннель — Phase 5; здесь только мост в контейнер-приложении.

Purpose: BRG-01/BRG-02 на iOS (Swift HostApi impl + event stream), BRG-04 (main-queue доставка + replay + снапшот). Закрывает Pitfall 4 (регистрация через applicationRegistrar, не rootViewController). Swift-файлы не пересекаются с Kotlin/Dart — план параллелен планам 02/05.
Output: AppDelegate (регистрация), VpnEventListener (StreamHandler), VpnHostApiImpl (echo), членство новых файлов в таргете Runner.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pigeon/01-RESEARCH.md
@.planning/phases/01-pigeon/01-01-SUMMARY.md
@ios/Runner/Bridge/Messages.g.swift
@ios/Runner/AppDelegate.swift
@ios/Runner/SceneDelegate.swift

<interfaces>
<!-- Из RESEARCH Code Example 4 (регистрация нового шаблона + echo StreamHandler). Точные имена символов (VpnHostApiSetup.setUp суффикс, регистр enum) — из 01-01-SUMMARY (Messages.g.swift). -->

Текущий AppDelegate (шаблон 3.44) уже реализует FlutterImplicitEngineDelegate и didInitializeImplicitFlutterEngine с GeneratedPluginRegistrant. Дополнить его регистрацией моста, не ломая существующее.

Сгенерированный код (Messages.g.swift):
- VpnHostApiSetup.setUp(binaryMessenger:api:)   // суффикс Setup — сверить по SUMMARY
- VpnEventsStreamHandler + VpnEventsStreamHandler.register(with:streamHandler:)
- PigeonEventSink<VpnEventMessage>; onListen(withArguments:sink:), onCancel(withArguments:)
- sealed/протокол VpnEventMessage + подтипы; LogMessage — поле level: String (свободная строка контракта, НЕ enum на native); Dart-маппер парсит её через LogLevel.values.byName
- enum VpnStatusMessage (.disconnected/.connecting/...)
- VpnStatusSnapshotMessage, VpnConfigMessage

didInitializeImplicitFlutterEngine(_ engineBridge:):
- GeneratedPluginRegistrant.register(with: engineBridge.pluginRegistry)   // уже есть
- let messenger = engineBridge.applicationRegistrar.messenger()
- VpnHostApiSetup.setUp(binaryMessenger: messenger, api: VpnHostApiImpl())
- VpnEventsStreamHandler.register(with: messenger, streamHandler: VpnEventListener.shared)

VpnEventListener: VpnEventsStreamHandler — static shared; eventSink; lastStatus (init .disconnected); onListen сохраняет sink + emit(lastStatus); onCancel sink=nil; emit(event){ if StatusChanged → lastStatus=…; DispatchQueue.main.async { sink?.success(event) } }
VpnHostApiImpl: VpnHostApi — startVpn(config, completion): эмит цепочки (LogMessage.level = "info") через VpnEventListener.shared.emit → completion(.success(())); stopVpn; getStatus() → snapshot из lastStatus + счётчики
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift эмиттер → Flutter engine (event channel) | события пересекают границу в Dart; требуется main queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Denial of Service | VpnEventListener.emit | mitigate | sink.success только внутри DispatchQueue.main.async; прямой вызов из другого потока запрещён (Pitfall 1) |
| T-1-06 | Spoofing (гонка статуса) | VpnEventListener.onListen | mitigate | onListen реплеит lastStatus сразу; getStatus() отдаёт снапшот (Pitfall 2, BRG-04) |
| T-1-07 | Tampering (регистрация) | AppDelegate | mitigate | messenger из engineBridge.applicationRegistrar.messenger() в didInitializeImplicitFlutterEngine; путь через rootViewController не использовать (Pitfall 4, #185935) |
| T-1-08 | Tampering (контракт level) | echo LogMessage.level | mitigate | level ставится строковым литералом нижнего регистра "info", совпадающим с именами Dart-enum LogLevel; верхний/смешанный регистр запрещён, иначе byName в маппере 01-03 бросит ArgumentError на live-прогоне (Plan 07) |
| T-1-01 | Information Disclosure | echo LogMessage-тексты | mitigate | Тексты синтетические; кред нет (VLESS — Phase 4) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: echo VpnEventListener и VpnHostApiImpl (Swift)</name>
  <files>ios/Runner/Bridge/VpnEventListener.swift, ios/Runner/Bridge/VpnHostApiImpl.swift</files>
  <read_first>
    - .planning/phases/01-pigeon/01-RESEARCH.md § «Code Example 4» + «Pattern 3/4» + «Pitfall 1/2»
    - .planning/phases/01-pigeon/01-01-SUMMARY.md (точные имена: VpnHostApiSetup.setUp, сигнатуры onListen(withArguments:sink:), регистр enum, форма @async completion, имена/порядок полей конструктора LogMessage)
    - ios/Runner/Bridge/Messages.g.swift (VpnEventsStreamHandler, PigeonEventSink, VpnStatusSnapshotMessage)
  </read_first>
  <action>
    Создать `VpnEventListener.swift` по Code Example 4 : подкласс/реализация `VpnEventsStreamHandler` — `static let shared`, `private var eventSink: PigeonEventSink<VpnEventMessage>?`, `private(set) var lastStatus` (init `.disconnected`); `onListen(withArguments:sink:)` сохраняет sink и `emit(lastStatus)`; `onCancel(withArguments:)` — `eventSink = nil`; `func emit(_ event)` — если `StatusChangedMessage`, обновить lastStatus (и хранить счётчики для snapshot), затем `DispatchQueue.main.async { [weak self] in self?.eventSink?.success(event) }`. Создать `VpnHostApiImpl.swift` : реализация `VpnHostApi` — `startVpn(config, completion)`: эмит цепочки `LogMessage("starting")`→`StatusChanged(.connecting)`→`LogMessage("tunnel up")`→`StatusChanged(.connected, connectedSinceEpochMs: now)` через `VpnEventListener.shared.emit`, затем `completion(.success(()))`; `stopVpn`: `.disconnecting`→`.disconnected`; `getStatus()`: собрать `VpnStatusSnapshotMessage` из lastStatus + счётчиков. КОНТРАКТ УРОВНЯ ЛОГА: поле `level` во всех echo-`LogMessage` — строковый литерал нижнего регистра `"info"` (совпадает с именами Dart-enum `LogLevel { info, warning, error }`); НЕ `"INFO"` и НЕ `"Info"`, иначе `LogLevel.values.byName` в маппере плана 01-03 бросит ArgumentError на live-прогоне (Plan 07). Точные имена/порядок полей конструктора `LogMessage` взять из `Messages.g.swift` и передать `level: "info"` явно. Реального NETunnelProviderManager/extension НЕ трогать (Phase 5). Точные сигнатуры @async completion взять из Messages.g.swift/SUMMARY. Без комментариев.
  </action>
  <verify>
    <automated>test -f ios/Runner/Bridge/VpnEventListener.swift && test -f ios/Runner/Bridge/VpnHostApiImpl.swift</automated>
  </verify>
  <acceptance_criteria>
    - Оба файла созданы под ios/Runner/Bridge/
    - `grep -c 'DispatchQueue.main' ios/Runner/Bridge/VpnEventListener.swift` больше 0 (доставка на main queue)
    - echo-`LogMessage` выставляют `level: "info"` (нижний регистр): `grep -c '"info"' ios/Runner/Bridge/VpnHostApiImpl.swift` больше 0; guard — `grep -c '"INFO"\|"Info"' ios/Runner/Bridge/VpnHostApiImpl.swift` возвращает 0 (верхний/смешанный регистр в level запрещён)
    - onListen реплеит lastStatus; getStatus собирает snapshot; NETunnelProviderManager/extension не задействованы
    - (Компиляция проверяется в Task 2 после добавления файлов в таргет)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: регистрация в AppDelegate и членство файлов в таргете Runner</name>
  <files>ios/Runner/AppDelegate.swift, ios/Runner.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/01-pigeon/01-RESEARCH.md § «Code Example 4» + «Pitfall 4» (регистрация через applicationRegistrar)
    - ios/Runner/AppDelegate.swift (текущий: уже есть didInitializeImplicitFlutterEngine с GeneratedPluginRegistrant)
    - ios/Runner/Bridge/Messages.g.swift + VpnHostApiImpl.swift + VpnEventListener.swift (Task 1)
  </read_first>
  <action>
    Дополнить существующий `didInitializeImplicitFlutterEngine(_ engineBridge:)` в AppDelegate.swift ПОСЛЕ `GeneratedPluginRegistrant.register`: `let messenger = engineBridge.applicationRegistrar.messenger()`, затем `VpnHostApiSetup.setUp(binaryMessenger: messenger, api: VpnHostApiImpl())` и `VpnEventsStreamHandler.register(with: messenger, streamHandler: VpnEventListener.shared)`. Существующий код метода и `application(_:didFinishLaunchingWithOptions:)` не ломать. Добавить три Swift-файла (`Bridge/Messages.g.swift`, `Bridge/VpnHostApiImpl.swift`, `Bridge/VpnEventListener.swift`) в build phase «Sources» таргета Runner в `ios/Runner.xcodeproj/project.pbxproj` (создать PBXFileReference + PBXBuildFile + записи в группе Runner и в Sources build phase; если проект использует синхронизированную группу-папку — убедиться, что Bridge/ попадает в неё). Проверить сборкой. Если pbxproj-правка не даёт линковку — зафиксировать в SUMMARY как известное ограничение для Plan 07 (ручное добавление в Xcode). Без комментариев в Swift.
  </action>
  <verify>
    <automated>flutter build ios --no-codesign --debug</automated>
  </verify>
  <acceptance_criteria>
    - `flutter build ios --no-codesign --debug` собирается без ошибок (Swift echo-мост входит в Runner и линкуется)
    - `grep -c 'applicationRegistrar.messenger' ios/Runner/AppDelegate.swift` больше 0 и `grep -c 'VpnEventsStreamHandler.register' ios/Runner/AppDelegate.swift` больше 0
    - Messages.g.swift и Bridge/*.swift присутствуют в Sources таргета Runner (сборка это подтверждает)
    - Регистрация идёт через applicationRegistrar, не через rootViewController
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `flutter build ios --no-codesign --debug` собирается
- Регистрация через applicationRegistrar.messenger() в didInitializeImplicitFlutterEngine
- События на main queue; replay lastStatus в onListen; snapshot в getStatus
- echo LogMessage.level = "info" (нижний регистр), совместимо с byName маппера 01-03
- Network Extension/entitlements не трогаются (граница фазы)
</verification>

<success_criteria>
iOS-echo моста компилируется и линкуется в Runner: HostApi зарегистрирован по новому шаблону, синтетические события идут через main-queue эмиттер, последний статус реплеится, снапшот доступен, level echo-логов совместим с Dart-enum. BRG-01/02/04 закрыты на iOS. Живая проверка на симуляторе — Plan 07 (опционально; при недоступности фиксируется как ограничение).
</success_criteria>

<output>
Create `.planning/phases/01-pigeon/01-06-SUMMARY.md` when done. Если членство файлов в таргете потребовало ручного шага в Xcode — записать это как вход для Plan 07.
</output>
</content>
