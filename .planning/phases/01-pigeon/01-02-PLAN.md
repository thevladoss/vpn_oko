---
phase: 01-pigeon
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - lib/features/vpn_connection/domain/entities/vpn_state.dart
  - lib/features/vpn_connection/domain/entities/vpn_config.dart
  - lib/features/vpn_connection/domain/entities/traffic_stats.dart
  - lib/features/vpn_connection/domain/repositories/vpn_repository.dart
  - lib/features/vpn_connection/domain/usecases/connect_vpn.dart
  - lib/features/vpn_connection/domain/usecases/disconnect_vpn.dart
  - lib/features/vpn_connection/domain/usecases/watch_vpn_state.dart
  - lib/features/vpn_connection/domain/usecases/sync_status.dart
  - lib/features/vpn_logs/domain/entities/log_entry.dart
  - lib/features/vpn_logs/domain/repositories/log_repository.dart
  - lib/features/vpn_logs/domain/usecases/watch_logs.dart
  - lib/core/error/failures.dart
  - test/features/vpn_connection/domain/entities/vpn_state_test.dart
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "VpnState — sealed с пятью подтипами, равенство по значению через equatable"
    - "Доменный слой не импортирует vpn_api.g.dart ни в одном файле"
    - "Ошибка выражается доменным VpnError с сообщением, а не строкой платформы"
  artifacts:
    - path: "lib/features/vpn_connection/domain/entities/vpn_state.dart"
      provides: "sealed VpnState: VpnDisconnected/VpnConnecting/VpnConnected/VpnDisconnecting/VpnError"
      contains: "sealed class VpnState"
      min_lines: 25
    - path: "lib/features/vpn_connection/domain/repositories/vpn_repository.dart"
      provides: "интерфейс VpnRepository (watchState/watchTraffic/connect/disconnect/syncStatus)"
      contains: "abstract"
    - path: "lib/features/vpn_logs/domain/entities/log_entry.dart"
      provides: "LogEntry + enum LogLevel {info,warning,error}"
      contains: "enum LogLevel"
    - path: "lib/core/error/failures.dart"
      provides: "sealed Failure для типизированных ошибок data-слоя"
      contains: "sealed class"
    - path: "test/features/vpn_connection/domain/entities/vpn_state_test.dart"
      provides: "тест равенства и иммутабельности VpnState (CORE-01)"
      contains: "VpnConnected"
  key_links:
    - from: "domain/entities/*"
      to: "package:equatable"
      via: "extends Equatable"
      pattern: "equatable"
    - from: "domain/*"
      to: "vpn_api.g.dart"
      via: "запрещённый импорт отсутствует"
      pattern: "vpn_api.g.dart"
---

<objective>
Зафиксировать доменное ядро до нативного кода: sealed/immutable модели, интерфейсы репозиториев и usecases всех фич, изолированные от кодогена. Это горизонтальная граница фазы — обе платформы и data-слой опираются на эти типы.

Purpose: CORE-01 — модели VpnState/VpnConfig/VpnEvent-домена sealed и тестируемы без платформы; ошибка доводится до доменного состояния Error. Чистый Dart, параллелится с нативным echo (планы 05/06).
Output: домен vpn_connection и vpn_logs, `core/error`, тест равенства VpnState.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-pigeon/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Целевые доменные типы из RESEARCH Code Example 8 + ARCHITECTURE. Чистый Dart, только equatable. -->

VpnState (sealed, extends Equatable):
- VpnDisconnected() · VpnConnecting() · VpnDisconnecting()  — props const []
- VpnConnected({required DateTime connectedSince})           — props [connectedSince]
- VpnError(String message)                                    — props [message]

VpnConfig({required String host, int port, String userId, String serverName}) — Equatable, immutable
TrafficStats({required int rxBytes, required int txBytes})                     — Equatable, immutable
LogEntry({required String text, required LogLevel level, required DateTime time}) — Equatable
enum LogLevel { info, warning, error }

VpnRepository (abstract):
- Stream<VpnState> watchState()
- Stream<TrafficStats> watchTraffic()
- Future<void> connect(VpnConfig config)
- Future<void> disconnect()
- Future<void> syncStatus()

LogRepository (abstract): Stream<LogEntry> watchLogs()

Usecases (по одному действию на класс, конструкторная инъекция repo):
- ConnectVpn(repo).call(VpnConfig) · DisconnectVpn(repo).call() · WatchVpnState(repo).call() · SyncStatus(repo).call()
- WatchLogs(logRepo).call()
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | Чистый Dart-домен, внешнего ввода и I/O нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | VpnError.message | mitigate | Доменная ошибка несёт человекочитаемое сообщение без секретов; строки платформы и любые креды в домен не попадают (маппинг в data-слое, Plan 04) |
| T-1-03 | Tampering | доменные модели | accept | Нет внешнего ввода, нет сети, нет десериализации; поверхность атаки отсутствует на этом слое |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Доменные модели vpn_connection и vpn_logs</name>
  <files>lib/features/vpn_connection/domain/entities/vpn_state.dart, lib/features/vpn_connection/domain/entities/vpn_config.dart, lib/features/vpn_connection/domain/entities/traffic_stats.dart, lib/features/vpn_logs/domain/entities/log_entry.dart, test/features/vpn_connection/domain/entities/vpn_state_test.dart</files>
  <read_first>
    - .planning/phases/01-pigeon/01-RESEARCH.md § «Code Example 8» (sealed VpnState + equatable)
    - .planning/codebase/CONVENTIONS.md (без комментариев; sealed+equatable; snake_case файлы)
  </read_first>
  <behavior>
    - VpnDisconnected() == VpnDisconnected() (равенство по значению, props const [])
    - VpnConnected(connectedSince: t) == VpnConnected(connectedSince: t); != при разном времени
    - VpnError('a') == VpnError('a'); != VpnError('b')
    - VpnConnecting() != VpnDisconnecting() (разные подтипы)
    - LogEntry с равными полями равны; LogLevel.values.byName('warning') == LogLevel.warning
  </behavior>
  <action>
    Создать `vpn_state.dart` по Code Example 8: `sealed class VpnState extends Equatable` с `const` конструктором и `props`; подтипы `VpnDisconnected`, `VpnConnecting`, `VpnDisconnecting` (props const []), `VpnConnected({required DateTime connectedSince})` (props [connectedSince]), `VpnError(String message)` (props [message]). Создать `vpn_config.dart` (`VpnConfig` — host/port/userId/serverName, immutable, Equatable) и `traffic_stats.dart` (`TrafficStats` — rxBytes/txBytes). Создать `log_entry.dart`: `enum LogLevel { info, warning, error }` и `LogEntry({text, level, time})` extends Equatable. Все файлы без комментариев, только импорт `package:equatable/equatable.dart`. Написать `vpn_state_test.dart` по блоку behavior ПЕРЕД реализацией, прогнать (RED→GREEN).
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/domain/entities/vpn_state_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `vpn_state_test.dart` зелёный; покрывает равенство всех пяти подтипов и различие по connectedSince/message
    - `grep -rL 'vpn_api.g.dart' lib/features/*/domain` — ни один доменный файл не импортирует генерированный код (проверка изоляции)
    - `flutter analyze lib/features/vpn_connection/domain lib/features/vpn_logs/domain` без errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Интерфейсы репозиториев, usecases, доменные Failure</name>
  <files>lib/features/vpn_connection/domain/repositories/vpn_repository.dart, lib/features/vpn_connection/domain/usecases/connect_vpn.dart, lib/features/vpn_connection/domain/usecases/disconnect_vpn.dart, lib/features/vpn_connection/domain/usecases/watch_vpn_state.dart, lib/features/vpn_connection/domain/usecases/sync_status.dart, lib/features/vpn_logs/domain/repositories/log_repository.dart, lib/features/vpn_logs/domain/usecases/watch_logs.dart, lib/core/error/failures.dart</files>
  <read_first>
    - lib/features/vpn_connection/domain/entities/vpn_state.dart (создан в Task 1)
    - .planning/research/ARCHITECTURE.md § «Component Responsibilities» + «Internal Boundaries» (инверсия зависимостей domain↔data)
  </read_first>
  <action>
    Создать `vpn_repository.dart` — abstract `VpnRepository` с сигнатурами из блока `<interfaces>` (`watchState`, `watchTraffic`, `connect`, `disconnect`, `syncStatus`). Создать usecases по одному действию на класс с конструкторной инъекцией `VpnRepository`: `ConnectVpn` (call принимает `VpnConfig`), `DisconnectVpn`, `WatchVpnState` (call возвращает `Stream<VpnState>`), `SyncStatus`. Создать `log_repository.dart` — abstract `LogRepository { Stream<LogEntry> watchLogs(); }` и usecase `WatchLogs`. Создать `core/error/failures.dart` — `sealed class Failure extends Equatable` с подтипами `PlatformFailure(String message)` и `VpnStartFailure(String code, String message)` для типизации ошибок платформы в data-слое (маппинг PlatformException — Plan 04). Без комментариев. Usecases не знают про data и pigeon.
  </action>
  <verify>
    <automated>flutter analyze lib/features/vpn_connection/domain lib/features/vpn_logs/domain lib/core/error</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` по указанным каталогам без errors
    - `grep -rl 'vpn_api.g.dart' lib/features/*/domain lib/core/error` пусто (изоляция домена сохранена; g.dart живёт только в core/bridge и features/*/data)
    - Каждый usecase принимает абстракцию репозитория через конструктор (SOLID, инверсия зависимостей)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `flutter test test/features/vpn_connection/domain` зелёный
- `flutter analyze` по доменным каталогам без errors
- Ни один файл домена не импортирует `vpn_api.g.dart`
</verification>

<success_criteria>
Доменное ядро зафиксировано: sealed/immutable модели с равенством, интерфейсы репозиториев, usecases, типизированные Failure. CORE-01 закрыт на уровне моделей и тестов; data-слой (планы 03/04) реализует интерфейсы.
</success_criteria>

<output>
Create `.planning/phases/01-pigeon/01-02-SUMMARY.md` when done.
</output>
