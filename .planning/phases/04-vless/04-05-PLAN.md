---
phase: 04-vless
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - lib/app/di.dart
  - lib/app/app.dart
  - lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart
  - test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart
autonomous: true
requirements: [VLS-02, VLS-03]
must_haves:
  truths:
    - "ServerConfigCubit зарегистрирован в MultiBlocProvider с инъекцией SystemClipboardSource + SocketLatencyProbe"
    - "Экран показывает кнопку «Вставить vless://»; тап вызывает pasteFromClipboard с haptic"
    - "В состоянии loaded экран показывает VlessConfigCard (реактивно, через BlocBuilder), иначе демо-ServerCard; в error — строку ошибки"
    - "Существующий vpn_home_screen_test остаётся зелёным после добавления ServerConfigCubit в дерево"
  artifacts:
    - path: "lib/app/di.dart"
      provides: "clipboardSource + latencyProbe в AppDependencies"
      contains: "SocketLatencyProbe"
    - path: "lib/app/app.dart"
      provides: "BlocProvider ServerConfigCubit в MultiBlocProvider"
      contains: "ServerConfigCubit"
    - path: "lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart"
      provides: "BlocBuilder<ServerConfigCubit> для карточки + PasteConfigButton"
      contains: "BlocBuilder<ServerConfigCubit"
  key_links:
    - from: "lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart"
      to: "ServerConfigCubit"
      via: "context.read<ServerConfigCubit>().pasteFromClipboard() из PasteConfigButton"
      pattern: "pasteFromClipboard"
    - from: "lib/app/di.dart"
      to: "SocketLatencyProbe + SystemClipboardSource"
      via: "инстанцирование в AppDependencies"
      pattern: "SocketLatencyProbe|SystemClipboardSource"
---

<objective>
## Phase Goal

**Как** пользователь Oko VPN, **я хочу** вставить `vless://`-ссылку из буфера, **чтобы** увидеть карточку распарсенного сервера с измеренной задержкой.

Интеграция feature `server_config` в экран фазы 3. Здесь вертикальный срез замыкается: DI собирает реальные реализации, `MultiBlocProvider` даёт `ServerConfigCubit`, `VpnHomeScreen` реактивно (`BlocBuilder`) показывает карточку и кнопку вставки. Минимально и неломающе — экран фазы 3 сохраняет поведение, существующий widget-тест остаётся зелёным.

Purpose: замыкание VLS-02/VLS-03 в UI. Вставленный конфиг — display-only (реальный Connect остаётся на демо-VpnConfig, вне требований фазы; см. RESEARCH Open Question 1).
Output: правки di/app/screen + обновлённый screen-тест.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-vless/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@lib/app/di.dart
@lib/app/app.dart
@lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart
@test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart

<interfaces>
<!-- Из 04-02: SystemClipboardSource() : ClipboardSource; SocketLatencyProbe({connector, timeout}) : LatencyProbe. -->
<!-- Из 04-03: VlessConfigCard({required VlessConfig config, LatencyResult? latency}); PasteConfigButton({required VoidCallback onPressed}); String describeVlessError(VlessError). -->
<!-- Из 04-04: ServerConfigCubit({required ClipboardSource clipboard, required LatencyProbe probe}); pasteFromClipboard();
     sealed ServerConfigState = ServerConfigInitial | ServerConfigError(VlessError error) | ServerConfigLoaded(VlessConfig config, {LatencyResult? latency}). -->

<!-- Текущий di.dart: класс AppDependencies с late final usecases + `final VpnConfig demoConfig`. -->
<!-- Текущий app.dart: MultiBlocProvider providers [VpnConnectionBloc(...), LogsCubit(...)] child VpnHomeScreen. -->
<!-- Текущий экран: блок `_Staggered(child: ServerCard(serverName: config.serverName, host: config.host, port: config.port))` между ирисом и трафиком; config = context.read<VpnConnectionBloc>().config (демо). -->
<!-- haptic: import 'package:flutter/services.dart'; HapticFeedback.mediumImpact() — паттерн DESIGN. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DI + провайдер ServerConfigCubit</name>
  <files>lib/app/di.dart, lib/app/app.dart</files>
  <read_first>
    - lib/app/di.dart (AppDependencies — куда добавить поля)
    - lib/app/app.dart (MultiBlocProvider — куда добавить BlocProvider)
    - lib/features/server_config/data/datasources/clipboard_source_impl.dart, data/probes/socket_latency_probe.dart (из 04-02)
  </read_first>
  <action>
    В `di.dart`: добавь в `AppDependencies` два поля — `final ClipboardSource clipboardSource = const SystemClipboardSource();` и `final LatencyProbe latencyProbe = const SocketLatencyProbe();` (импорты domain-абстракций + data-реализаций). Ничего в существующей сборке моста/usecases не трогай.
    В `app.dart`: добавь третий `BlocProvider` в `providers` — `BlocProvider(create: (_) => ServerConfigCubit(clipboard: dependencies.clipboardSource, probe: dependencies.latencyProbe))`. Порядок: рядом с LogsCubit. Никаких изменений VpnConnectionBloc.
  </action>
  <acceptance_criteria>
    - `flutter analyze lib/app` чистый
    - `grep -c "SocketLatencyProbe" lib/app/di.dart` == 1 и `grep -c "SystemClipboardSource" lib/app/di.dart` == 1
    - `grep -c "ServerConfigCubit" lib/app/app.dart` == 1
    - VpnConnectionBloc.config по-прежнему = demoConfig (не заменён вставленным конфигом)
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/app</automated>
  </verify>
  <done>ServerConfigCubit доступен во всём дереве с реальными ClipboardSource + LatencyProbe</done>
</task>

<task type="auto">
  <name>Task 2: BlocBuilder карточки + кнопка вставки в экране + обновление теста</name>
  <files>
    lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart,
    test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart
  </files>
  <read_first>
    - .planning/phases/04-vless/04-RESEARCH.md → «Интеграция в экран фазы 3» (шаги 1-4), «Pitfall 4» (карточка обязана быть реактивной, не context.read)
    - lib/features/vpn_connection/presentation/screens/vpn_home_screen.dart (блок _Staggered → ServerCard, строки ~119-127)
    - test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart (pumpScreen — MultiBlocProvider с VpnConnectionBloc + LogsCubit)
  </read_first>
  <action>
    В экране замени блок `_Staggered(child: ServerCard(...))` на `_Staggered(child: BlocBuilder<ServerConfigCubit, ServerConfigState>(builder: (context, cfgState) => ...))`. Внутри builder — switch по `cfgState`:
    - `ServerConfigLoaded(:final config, :final latency)` → `VlessConfigCard(config: config, latency: latency)`;
    - `ServerConfigError(:final error)` → Column[ демо `ServerCard(serverName: config.serverName, host: config.host, port: config.port)`, строка `describeVlessError(error)` стилем bodySmall/accentError ];
    - `ServerConfigInitial()` → демо `ServerCard(...)`.
    Под карточкой (или в её блоке) добавь `PasteConfigButton(onPressed: () { HapticFeedback.mediumImpact(); context.read<ServerConfigCubit>().pasteFromClipboard(); })`. `config` для демо-ветки берётся как сейчас (`context.read<VpnConnectionBloc>().config`). Не ломай staggered-анимацию и остальные зоны экрана.
    В тесте: в `pumpScreen` добавь третий провайдер `BlocProvider<ServerConfigCubit>.value(value: cubit)` где cubit собран из фейков (FakeClipboardSource + FakeLatencyProbe из test/helpers). Существующие два теста должны остаться зелёными (Echo Server карточка видна в Initial). Добавь тест: PasteConfigButton присутствует; после того как фейк-clipboard отдаёт валидную vless-ссылку и cubit.pasteFromClipboard() отрабатывает (pumpAndSettle), в дереве появляется `VlessConfigCard`, а полный uuid отсутствует.
  </action>
  <acceptance_criteria>
    - `flutter test test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart` зелёный (старые 2 теста + новый)
    - `grep -c "BlocBuilder<ServerConfigCubit" lib/...vpn_home_screen.dart` == 1
    - `grep -c "context.read<ServerConfigCubit>().pasteFromClipboard" lib/...vpn_home_screen.dart` >= 1
    - Карточка НЕ читается через context.read для отображения (реактивность через BlocBuilder — Pitfall 4)
    - Полный uuid не появляется в дереве экрана (проверка в новом тесте)
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/screens/vpn_home_screen_test.dart</automated>
  </verify>
  <done>Экран реактивно показывает карточку/ошибку/демо + кнопку вставки; вставка обновляет карточку; существующие тесты зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| пользователь (тап «Вставить») → cubit → сеть | пользователь инициирует чтение буфера и tcping |
| VlessConfig → экран | uuid проходит в UI-дерево |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Information Disclosure | VpnHomeScreen карточка | mitigate | карточка рендерит только VlessConfigCard (маскирует uuid); тест экрана доказывает отсутствие полного uuid |
| T-4-04 | (SSRF-подобный) | pasteFromClipboard → measure | accept | tcping только по явному тапу пользователя; таймаут пробы 3с; авто-вставки/авто-коннекта нет |
| T-4-02b | Information Disclosure | VpnConnectionBloc.config | accept | вставленный конфиг display-only, не проводится в реальный Connect (демо-config неизменен) — вне требований фазы (Open Q1) |
</threat_model>

<verification>
- `flutter test test/features/vpn_connection/` зелёный (экран не сломан)
- `flutter analyze lib/app lib/features/vpn_connection/presentation/screens` чистый
- Вертикальный срез замкнут: вставка → парс → карточка с задержкой в реальном дереве
</verification>

<success_criteria>
VLS-02/VLS-03 замкнуты в UI: кнопка вставки вызывает paste→parse→measure, карточка обновляется реактивно, uuid маскирован, экран фазы 3 сохраняет поведение и тесты.
</success_criteria>

<output>
Create `.planning/phases/04-vless/04-05-SUMMARY.md` when done
</output>
