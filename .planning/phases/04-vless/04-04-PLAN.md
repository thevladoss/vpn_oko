---
phase: 04-vless
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/features/server_config/presentation/cubit/server_config_state.dart
  - lib/features/server_config/presentation/cubit/server_config_cubit.dart
  - test/helpers/fake_clipboard_source.dart
  - test/helpers/fake_latency_probe.dart
  - test/features/server_config/presentation/server_config_cubit_test.dart
autonomous: true
requirements: [VLS-02]
must_haves:
  truths:
    - "pasteFromClipboard: пустой буфер → state error(empty); валидная ссылка → loaded(config) затем loaded(config, latency)"
    - "Кривая ссылка → state error(reason) с точной причиной парсера, без падения"
    - "После loaded вызывается probe.measure(host, port) и результат доезжает в state"
  artifacts:
    - path: "lib/features/server_config/presentation/cubit/server_config_state.dart"
      provides: "sealed ServerConfigState (Initial|Error|Loaded)"
      contains: "sealed class ServerConfigState"
    - path: "lib/features/server_config/presentation/cubit/server_config_cubit.dart"
      provides: "ServerConfigCubit оркеструет paste→parse→measure"
      contains: "class ServerConfigCubit extends Cubit"
    - path: "test/features/server_config/presentation/server_config_cubit_test.dart"
      provides: "blocTest: valid/invalid/empty/measure (VLS-02)"
      min_lines: 50
  key_links:
    - from: "lib/features/server_config/presentation/cubit/server_config_cubit.dart"
      to: "lib/features/server_config/domain/services/vless_parser.dart"
      via: "parseVless(raw)"
      pattern: "parseVless\\("
    - from: "lib/features/server_config/presentation/cubit/server_config_cubit.dart"
      to: "domain LatencyProbe + ClipboardSource"
      via: "конструкторная инъекция абстракций"
      pattern: "LatencyProbe|ClipboardSource"
---

<objective>
## Phase Goal

**Как** пользователь Oko VPN, **я хочу** вставить `vless://`-ссылку из буфера, **чтобы** увидеть карточку распарсенного сервера с измеренной задержкой.

Presentation-оркестратор feature `server_config`: `ServerConfigCubit` связывает `ClipboardSource` → `parseVless` → `LatencyProbe` и эмитит sealed `ServerConfigState`. Cubit зависит только от domain-абстракций (реализации инъектит DI в 04-05) — тестируется фейками без сети и platform channel.

Purpose: логическое ядро VLS-02 (paste→parse→measure), test-first через bloc_test.
Output: state + cubit + 2 фейка-хелпера + bloc-тест.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-vless/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@lib/features/vpn_logs/presentation/bloc/logs_cubit.dart
@lib/features/vpn_logs/presentation/bloc/logs_state.dart

<interfaces>
<!-- Из 04-01: parseVless(String)->VlessParseResult (VlessParsed(config)|VlessParseFailure(error));
     VlessError{empty,malformed,scheme,uuid,host,port}; VlessConfig; LatencyResult;
     abstract interface LatencyProbe.measure(host,port)->Future<LatencyResult>;
     abstract interface ClipboardSource.readText()->Future<String?>. -->

<!-- Эталон sealed-состояния (lib/features/vpn_connection/domain/entities/vpn_state.dart):
     sealed class ... extends Equatable, props => const []; подклассы с const-конструкторами. -->

<!-- Эталон Cubit lifecycle (logs_cubit.dart): extends Cubit<State>, super(const Initial), emit(...). -->

<!-- bloc_test ^10 в pubspec: blocTest<Cubit,State>('...', build:, act:, expect: () => [...]). mocktail ^1.0.5 доступен. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ServerConfigState + стаб-cubit + фейки + падающий bloc-тест (RED)</name>
  <files>
    lib/features/server_config/presentation/cubit/server_config_state.dart,
    lib/features/server_config/presentation/cubit/server_config_cubit.dart,
    test/helpers/fake_clipboard_source.dart,
    test/helpers/fake_latency_probe.dart,
    test/features/server_config/presentation/server_config_cubit_test.dart
  </files>
  <read_first>
    - .planning/phases/04-vless/04-RESEARCH.md → «Pattern 4: ServerConfigCubit оркеструет paste→parse→measure»
    - lib/features/vpn_connection/domain/entities/vpn_state.dart (sealed эталон)
    - test/features/vpn_logs/presentation/bloc/logs_cubit_test.dart (стиль фейков и pumpEventQueue)
    - test/helpers/mock_vpn_usecases.dart (расположение и стиль тест-хелперов)
  </read_first>
  <behavior>
    RED-тест обязан КОМПИЛИРОВАТЬСЯ и падать на ассертах, а не на отсутствии класса. Поэтому cubit создаётся стабом (эмитит только Initial), и blocTest не сходится с ожидаемыми списками. Сценарии blocTest:
    - пустой буфер (readText → null или '  ') → [ServerConfigError(VlessError.empty)]
    - валидная ссылка + probe→LatencyMeasured(56ms) → [ServerConfigLoaded(config), ServerConfigLoaded(config, latency: LatencyMeasured(56ms))]
    - валидная ссылка + probe→LatencyUnreachable → [ServerConfigLoaded(config), ServerConfigLoaded(config, latency: LatencyUnreachable())]
    - кривая ссылка (readText → 'https://x') → [ServerConfigError(VlessError.scheme)]; probe.measure НЕ вызывается
  </behavior>
  <action>
    `server_config_state.dart`: `sealed class ServerConfigState extends Equatable` (props => const []); `ServerConfigInitial()` (const); `ServerConfigError(VlessError error)` (props=[error]); `ServerConfigLoaded(VlessConfig config, {LatencyResult? latency})` (props=[config, latency]).
    `server_config_cubit.dart` — КОМПИЛИРУЮЩИЙСЯ СТАБ (не заглушка-throw): `class ServerConfigCubit extends Cubit<ServerConfigState>`; конструктор `ServerConfigCubit({required this.clipboard, required this.probe}) : super(const ServerConfigInitial())`; поля `final ClipboardSource clipboard; final LatencyProbe probe;`; метод `Future<void> pasteFromClipboard() async {}` — пустое тело, ничего не эмитит. Стаб нужен, чтобы тест-файл компилировался и `flutter analyze` был чистым; ассерты blocTest не сойдутся (эмиссий нет) → это и есть RED. Реальную логику даёт Task 2.
    `test/helpers/fake_clipboard_source.dart`: `class FakeClipboardSource implements ClipboardSource` с настраиваемым `String? textToReturn`; `readText()` возвращает его.
    `test/helpers/fake_latency_probe.dart`: `class FakeLatencyProbe implements LatencyProbe` с настраиваемым `LatencyResult resultToReturn` и счётчиком вызова `measure` (запомнить последние host/port, чтобы проверить что measure не вызван на error-кейсе).
    `server_config_cubit_test.dart`: blocTest по сценариям behavior, build инъектит фейки, act вызывает `cubit.pasteFromClipboard()`. На кривой ссылке проверь `fakeProbe.measureCallCount == 0`.
    Без комментариев во всех файлах.
  </action>
  <acceptance_criteria>
    - `flutter analyze lib/features/server_config/presentation/cubit test/features/server_config/presentation test/helpers/fake_clipboard_source.dart test/helpers/fake_latency_probe.dart` ЧИСТЫЙ (стаб-cubit компилируется, класс существует)
    - `flutter test test/features/server_config/presentation/server_config_cubit_test.dart` на этом шаге ПАДАЕТ на ассертах (стаб эмитит только Initial, ожидаемые списки не сходятся) — RED зафиксирован коммитом `test(04-04): ...`; падение НЕ compile-error
    - `server_config_state.dart` компилируется; `grep -c "sealed class ServerConfigState"` == 1
    - `grep -c "class ServerConfigCubit extends Cubit" lib/...server_config_cubit.dart` == 1 (стаб есть)
    - Фейки лежат в test/helpers, реализуют domain-абстракции, ноль обращений к реальной сети/Clipboard
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/features/server_config/presentation/cubit test/features/server_config/presentation test/helpers/fake_clipboard_source.dart test/helpers/fake_latency_probe.dart</automated>
  </verify>
  <done>State, стаб-cubit и фейки готовы и компилируются; bloc-тест написан и красный по ассертам (ожидает реализацию cubit в Task 2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ServerConfigCubit — реализация стаба до зелёного (GREEN, VLS-02)</name>
  <files>lib/features/server_config/presentation/cubit/server_config_cubit.dart</files>
  <read_first>
    - .planning/phases/04-vless/04-RESEARCH.md → «Pattern 4» (алгоритм pasteFromClipboard)
    - lib/features/server_config/presentation/cubit/server_config_cubit.dart (стаб из Task 1 — заполняем тело pasteFromClipboard)
    - lib/features/server_config/presentation/cubit/server_config_state.dart (из Task 1)
    - lib/features/server_config/domain/services/vless_parser.dart, latency_probe.dart, clipboard_source.dart
  </read_first>
  <behavior>
    Реализация делает Task-1 тесты зелёными. Порядок эмиссии для валидной ссылки: сначала loaded(config), затем loaded(config, latency) после measure.
  </behavior>
  <action>
    Замени пустое тело `pasteFromClipboard()` в стабе (конструктор и поля из Task 1 не трогай) на реальную логику: `final raw = await clipboard.readText();` если `raw == null || raw.trim().isEmpty` → `emit(const ServerConfigError(VlessError.empty))` и return. Иначе `switch (parseVless(raw))`: `VlessParseFailure(:final error) => emit(ServerConfigError(error))`; `VlessParsed(:final config) => { emit(ServerConfigLoaded(config)); final latency = await probe.measure(config.host, config.port); emit(ServerConfigLoaded(config, latency: latency)); }`. На failure-ветке probe НЕ трогается. Никакого uuid в логах/emit кроме внутри config (карточка маскирует). Без комментариев.
  </action>
  <acceptance_criteria>
    - `flutter test test/features/server_config/presentation/server_config_cubit_test.dart` зелёный (все 4 сценария)
    - `flutter test test/features/server_config/` зелёный (парсер + проба + cubit + карточка предыдущих планов не сломаны)
    - Cubit импортирует только domain (parser + абстракции) + flutter_bloc; grep import не содержит data/ путей
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/server_config/presentation/server_config_cubit_test.dart</automated>
  </verify>
  <done>ServerConfigCubit оркеструет paste→parse→measure; 4 сценария blocTest зелёные (VLS-02)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ClipboardSource → cubit → parseVless | недоверенная строка из буфера входит в оркестрацию |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Denial of Service | ServerConfigCubit.pasteFromClipboard | mitigate | пустой/кривой ввод → sealed ServerConfigError, cubit не бросает; blocTest фиксирует empty/scheme без throw |
| T-4-02 | Information Disclosure | ServerConfigState | mitigate | uuid не логируется и не выносится в отдельное поле state — живёт только внутри VlessConfig, карточка маскирует |
</threat_model>

<verification>
- `flutter test test/features/server_config/` зелёный целиком
- Cubit зависит только от domain-абстракций (SOLID; DI подключит реализации в 04-05)
</verification>

<success_criteria>
Логика VLS-02 готова: cubit разбирает вставку, эмитит loaded/error, меряет задержку через инъектированную пробу; 4 сценария blocTest зелёные, ноль сети/платформы в тесте.
</success_criteria>

<output>
Create `.planning/phases/04-vless/04-04-SUMMARY.md` when done
</output>
