---
phase: 04-vless
plan: 06
type: execute
wave: 4
depends_on: [04-05]
files_modified: []
autonomous: false
requirements: [VLS-01, VLS-02, VLS-03, QA-01]
must_haves:
  truths:
    - "Полный flutter test зелёный, flutter analyze чистый (very_good_analysis)"
    - "На устройстве/эмуляторе вставка реальной vless://-ссылки показывает карточку конфига"
    - "Карточка показывает измеренную задержку (мс) или «недоступен»; uuid замаскирован"
    - "Кривая ссылка даёт понятную ошибку, приложение не падает"
  artifacts: []
  key_links: []
---

<objective>
## Phase Goal

**Как** пользователь Oko VPN, **я хочу** вставить `vless://`-ссылку из буфера, **чтобы** увидеть карточку распарсенного сервера с измеренной задержкой.

Phase-gate фазы 4: сначала автоматический зелёный gate (весь тест-набор + анализатор), затем живая device-проверка вертикального среза — вставка реальной ссылки, карточка, реальный tcping. Это единственная ручная проверка фазы (сеть и буфер ОС недоступны автосьюту).

Purpose: доказать VLS-01/02/03 + QA-01 на реальном устройстве перед `/gsd:verify-work`.
Output: подтверждённый phase-gate.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-vless/04-VALIDATION.md
@.planning/phases/04-vless/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полный автоматический phase-gate</name>
  <files></files>
  <read_first>
    - .planning/phases/04-vless/04-VALIDATION.md → «Sampling Rate» (phase-gate: full flutter test + flutter analyze)
  </read_first>
  <action>
    Прогони весь набор и анализатор. Если что-то красное — почини в соответствующем файле до зелёного (не в этом плане создавать новую логику; только устранить регресс). Убедись, что все тесты server_config и существующие тесты фазы 3 зелёные.
  </action>
  <acceptance_criteria>
    - `flutter test` — весь набор зелёный (server_config: парсер 11 кейсов, проба, cubit 4 сценария, карточка; vpn_connection/vpn_logs без регресса)
    - `flutter analyze` — ноль ошибок и предупреждений (very_good_analysis)
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>Автоматический gate зелёный: весь тест-набор проходит, анализатор чист</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device-проверка вставки vless:// и tcping (VLS-01/02/03, QA-01)</name>
  <files></files>
  <action>
    Приостановить исполнение и передать пользователю device-чеклист ниже. Дождаться «approved» или списка дефектов; кода задача не пишет. При дефектах — gap-closure план на исправление.
  </action>
  <what-built>
    Feature server_config: вставка vless://-ссылки из буфера → парсинг → карточка конфига (name, host:port, transport, security, sni, замаскированный uuid) + реальный tcping-замер задержки. Кнопка «Вставить vless://» в VpnHomeScreen. Кривые ссылки дают понятную ошибку.
  </what-built>
  <how-to-verify>
    1. Запусти на эмуляторе/устройстве Android: `flutter run` (эмулятор API 34+ уже используется в проекте).
    2. Скопируй в буфер реальную рабочую vless://-ссылку (например от своего сервера; percent-encoded имя/IPv6 — по желанию).
    3. Нажми кнопку «Вставить vless://» на экране. Ожидается: демо-карточка Echo Server сменяется карточкой распарсенного конфига — имя сервера, адрес host:port, transport/security, sni; uuid показан в маскированной форме (первые 8 символов … последние 4), НЕ полностью.
    4. В течение ~1-3с на карточке появляется задержка: «· NN ms» (для достижимого хоста) или «· недоступен» (для недостижимого/таймаут 3с).
    5. Скопируй заведомо кривую строку (например `https://example.com` или обрезанную vless://) и снова нажми вставку. Ожидается: понятная строка ошибки под карточкой (например «Это не vless://-ссылка» / «Ссылка повреждена»), приложение НЕ падает.
    6. Проверь логи/консоль: полный uuid нигде не печатается.
  </how-to-verify>
  <acceptance_criteria>
    - Пункты 1-6 подтверждены на устройстве, либо перечислены конкретные дефекты для gap-closure
    - Маскировка uuid, реальная задержка и обработка кривой ссылки работают вживую
  </acceptance_criteria>
  <resume-signal>Напиши «approved», если карточка, задержка, маскировка uuid и обработка ошибки работают; иначе опиши, что пошло не так</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| буфер ОС → приложение → сеть | реальный недоверенный ввод и реальный tcping на устройстве |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Denial of Service | вставка кривой ссылки | mitigate | шаг 5 device-проверки: приложение не падает на кривом вводе |
| T-4-02 | Information Disclosure | карточка + логи | mitigate | шаг 3 и 6: uuid маскирован в UI и не печатается в логах |
| T-4-03 | Tampering | percent-decode | mitigate | шаг 2-3: percent-encoded имя отображается корректно |
| T-4-04 | (SSRF-подобный) | tcping | accept | коннект по явному тапу; таймаут 3с (шаг 4) |
</threat_model>

<verification>
- Автоматический gate: `flutter analyze` чистый + `flutter test` весь зелёный
- Ручной gate: вставка реальной ссылки → карточка + задержка + маскировка uuid; кривая ссылка → ошибка без падения
</verification>

<success_criteria>
Все четыре критерия успеха фазы 4 подтверждены живьём: карточка конфига после вставки, понятная ошибка на кривой ссылке, измеренная задержка, зелёные unit-тесты парсера (VLS-01, VLS-02, VLS-03, QA-01).
</success_criteria>

<output>
Create `.planning/phases/04-vless/04-06-SUMMARY.md` when done
</output>
