---
phase: 04-vless
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/features/server_config/presentation/widgets/vless_config_card.dart
  - lib/features/server_config/presentation/widgets/paste_config_button.dart
  - lib/features/server_config/presentation/widgets/vless_error_text.dart
  - test/features/server_config/presentation/vless_config_card_test.dart
autonomous: true
requirements: [VLS-02]
must_haves:
  truths:
    - "VlessConfigCard показывает name, host:port, transport, security, sni и замаскированный uuid"
    - "Полный uuid никогда не появляется в карточке; показывается только maskUuid-форма"
    - "Задержка отображается: '· NN ms' при LatencyMeasured, '· недоступен' при LatencyUnreachable, пусто при null"
    - "describeVlessError даёт понятный русский текст для каждого VlessError"
  artifacts:
    - path: "lib/features/server_config/presentation/widgets/vless_config_card.dart"
      provides: "VlessConfigCard (StatelessWidget, props config+latency) + maskUuid"
      contains: "class VlessConfigCard extends StatelessWidget"
    - path: "lib/features/server_config/presentation/widgets/paste_config_button.dart"
      provides: "PasteConfigButton (StatelessWidget, onPressed callback)"
      contains: "class PasteConfigButton extends StatelessWidget"
    - path: "lib/features/server_config/presentation/widgets/vless_error_text.dart"
      provides: "String describeVlessError(VlessError)"
      contains: "String describeVlessError"
    - path: "test/features/server_config/presentation/vless_config_card_test.dart"
      provides: "widget-тест: поля, маскировка uuid, варианты задержки (VLS-02)"
      min_lines: 40
  key_links:
    - from: "lib/features/server_config/presentation/widgets/vless_config_card.dart"
      to: "lib/features/server_config/domain/entities/vless_config.dart"
      via: "props типа VlessConfig + LatencyResult?"
      pattern: "VlessConfig"
---

<objective>
## Phase Goal

**Как** пользователь Oko VPN, **я хочу** вставить `vless://`-ссылку из буфера, **чтобы** увидеть карточку распарсенного сервера с измеренной задержкой.

Презентационные виджеты feature `server_config`: карточка конфига `VlessConfigCard` (dumb, поверх `OkoTones`, маскирует uuid), кнопка вставки `PasteConfigButton` (dumb, callback), маппер ошибок `describeVlessError`. Реактивность (`BlocBuilder`) добавит интеграционный план 04-05 — виджеты остаются чистыми и тестируемыми в изоляции по паттерну существующего `ServerCard`/`ConnectButton`.

Purpose: визуальная часть VLS-02. Маскировка uuid (Security Domain V7).
Output: 3 виджет-файла + widget-тест.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DESIGN.md
@.planning/phases/04-vless/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@lib/features/vpn_connection/presentation/widgets/server_card.dart
@lib/core/theme/oko_tones.dart

<interfaces>
<!-- Из 04-01: VlessConfig{uuid,host,port,transport,security,sni?,name}; VlessError{empty,malformed,scheme,uuid,host,port};
     LatencyResult = LatencyMeasured(Duration rtt) | LatencyUnreachable(). -->

<!-- Токены темы (lib/core/theme/oko_tones.dart), доступ через context.okoTones: -->
tones.surfaceCard, tones.textPrimary, tones.textSecondary, tones.accentError,
tones.accentConnected /* зелёный */, tones.accentTransitional /* янтарь */

<!-- Эталон карточки (server_card.dart): Container padding EdgeInsets.all(24), BoxDecoration surfaceCard,
     borderRadius 20; Row[Icon(Icons.dns_rounded), Column[name bodyMedium w600 textPrimary, host:port bodySmall textSecondary]]. -->

<!-- Эталон кнопки-callback (connect_button.dart): StatelessWidget, принимает VoidCallback, БЕЗ context.read внутри. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VlessConfigCard, PasteConfigButton, describeVlessError</name>
  <files>
    lib/features/server_config/presentation/widgets/vless_config_card.dart,
    lib/features/server_config/presentation/widgets/paste_config_button.dart,
    lib/features/server_config/presentation/widgets/vless_error_text.dart
  </files>
  <read_first>
    - .planning/phases/04-vless/04-RESEARCH.md → «Маскировка UUID», «Интеграция в экран фазы 3» (стиль карточки, токены), «Common Pitfall 7» (IPv6 host в отображении), «Anti-Patterns» (показ полного uuid запрещён)
    - lib/features/vpn_connection/presentation/widgets/server_card.dart (эталон верстки карточки)
    - lib/features/vpn_connection/presentation/widgets/connect_button.dart (эталон callback-кнопки)
    - .planning/DESIGN.md → строка 50 (карточка сервера: имя, адрес:порт, без спиннеров)
  </read_first>
  <action>
    `vless_config_card.dart`: top-level `String maskUuid(String uuid) => uuid.length >= 12 ? '${uuid.substring(0,8)}…${uuid.substring(uuid.length-4)}' : '••••';`. `VlessConfigCard extends StatelessWidget` с props `required VlessConfig config` и `LatencyResult? latency`. Верстка по образцу `ServerCard` через `context.okoTones`: заголовок `config.name` (textPrimary, w600); строка адреса — host:port с IPv6-обёрткой `config.host.contains(':') ? '[${config.host}]:${config.port}' : '${config.host}:${config.port}'` (Pitfall 7); строка метаданных `config.transport · config.security` и `config.sni` если не null; строка uuid — `maskUuid(config.uuid)` (textSecondary). Задержка справа/под адресом: `switch (latency) { LatencyMeasured(:final rtt) => '· ${rtt.inMilliseconds} ms', LatencyUnreachable() => '· недоступен', null => '' }`; цвет опционально по порогам (<100 accentConnected, <300 accentTransitional, иначе accentError) — на усмотрение исполнителя, дефолт textSecondary. Полный uuid НЕ рендерить нигде.
    `paste_config_button.dart`: `PasteConfigButton extends StatelessWidget` с `required VoidCallback onPressed`; кнопка (`FilledButton.tonalIcon` или в стиле проекта) с иконкой `Icons.content_paste_rounded` и текстом «Вставить vless://». Внутри НЕ вызывать context.read — только onPressed (haptic и cubit подключит экран 04-05).
    `vless_error_text.dart`: top-level `String describeVlessError(VlessError e)` — switch по всем значениям с русским текстом: empty→«Буфер пуст», malformed→«Ссылка повреждена», scheme→«Это не vless://-ссылка», uuid→«Неверный UUID», host→«Не указан хост», port→«Неверный порт». Без комментариев.
  </action>
  <acceptance_criteria>
    - `flutter analyze lib/features/server_config/presentation/widgets` чистый
    - `grep -c "config.uuid" lib/...vless_config_card.dart` показывает uuid используется только внутри maskUuid (нет прямого Text(config.uuid))
    - `describeVlessError` — exhaustive switch по VlessError (компилятор требует все ветки)
    - PasteConfigButton не содержит `context.read` (grep == 0)
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/features/server_config/presentation/widgets</automated>
  </verify>
  <done>Карточка, кнопка вставки и маппер ошибок готовы; uuid маскируется, IPv6 корректно отображается</done>
</task>

<task type="auto">
  <name>Task 2: Widget-тест карточки — поля, маскировка, задержка (VLS-02)</name>
  <files>test/features/server_config/presentation/vless_config_card_test.dart</files>
  <read_first>
    - test/features/vpn_connection/presentation/widgets/status_badge_test.dart (эталон widget-теста: host() с OkoTheme.dark, find.text)
    - .planning/phases/04-vless/04-VALIDATION.md → строка «карточка рендерит поля, маскирует uuid»
  </read_first>
  <action>
    Оберни карточку в `MaterialApp(theme: OkoTheme.dark, home: Scaffold(body: Center(child: VlessConfigCard(config: ..., latency: ...))))` по образцу status_badge_test.
    Кейсы: (1) с полным конфигом (uuid `b831381d-6324-4d53-ad4f-8cda48b30811`, host example.com, port 443, transport tcp, security reality, sni www.microsoft.com, name Tokyo) — `find.text('Tokyo')`, адрес `example.com:443` присутствует, transport/security видны; ПОЛНЫЙ uuid отсутствует (`find.textContaining('b831381d-6324-4d53')` → findsNothing), маскированная форма присутствует (`find.textContaining('b831381d')` findsOneWidget, содержит `…`). (2) latency=LatencyMeasured(56ms) → `find.textContaining('56 ms')`. (3) latency=LatencyUnreachable() → `find.textContaining('недоступен')`. (4) IPv6 config host `2606:4700:4700::1111` port 8443 → адрес `[2606:4700:4700::1111]:8443` присутствует.
    Плюс мелкий тест `describeVlessError`: для каждого VlessError результат не пуст (isNotEmpty).
  </action>
  <acceptance_criteria>
    - `flutter test test/features/server_config/presentation/vless_config_card_test.dart` зелёный
    - Тест доказывает отсутствие полного uuid и присутствие маскированной формы
    - Покрыты оба варианта LatencyResult + null и IPv6-адрес
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/server_config/presentation/vless_config_card_test.dart</automated>
  </verify>
  <done>Widget-тест зелёный: карточка рендерит поля, маскирует uuid, показывает задержку; describeVlessError покрыт</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VlessConfig → UI-рендеринг | секретный uuid проходит в презентацию |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Information Disclosure | VlessConfigCard | mitigate | uuid рендерится только через maskUuid; widget-тест доказывает отсутствие полной строки в дереве (Security V7) |
</threat_model>

<verification>
- `flutter test test/features/server_config/presentation/vless_config_card_test.dart` зелёный
- `flutter analyze lib/features/server_config/presentation/widgets` чистый
- Карточка и кнопка — dumb-виджеты, готовы к обёртке BlocBuilder в 04-05
</verification>

<success_criteria>
Визуальная часть VLS-02 готова: карточка показывает поля конфига и задержку, uuid замаскирован (доказано тестом), кнопка вставки — чистый callback-виджет.
</success_criteria>

<output>
Create `.planning/phases/04-vless/04-03-SUMMARY.md` when done
</output>
