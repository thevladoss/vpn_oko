---
phase: 02-android-vpnservice
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - lib/app/di.dart
  - lib/main.dart
autonomous: true
requirements: [AND-05]

must_haves:
  truths:
    - "Debug-harness показывает живые счётчики rx/tx из событий trafficChanged"
    - "Счётчики обновляются по мере роста rx (для наблюдения при ping в подсеть туннеля)"
  artifacts:
    - path: "lib/app/di.dart"
      provides: "Экспонирование потока TrafficStats из vpnRepository в harness"
      contains: "watchTraffic"
    - path: "lib/main.dart"
      provides: "StreamBuilder<TrafficStats> с readout rx/tx в DebugHarness"
      contains: "TrafficStats"
  key_links:
    - from: "lib/main.dart"
      to: "vpnRepository.watchTraffic()"
      via: "StreamBuilder на traffic-поток из AppDependencies"
      pattern: "watchTraffic"
---

<objective>
Сделать счётчики трафика видимыми в существующем debug-harness — иначе device-чеклист «rx растёт в UI» (AND-05) нечем подтвердить. Data-слой уже полностью прокинут в фазе 1 (`VpnNativeDatasource.traffic`, `VpnRepositoryImpl.watchTraffic()`, маппер `trafficToEntity`); недостаёт только экспонирования потока в `AppDependencies` и readout'а в harness. Это точечная правка инструмента проверки, НЕ экран Phase 3 (Bloc/DESIGN.md вне границ фазы).

Purpose: сделать AND-05 наблюдаемым в harness для phase-gate.
Output: `watchTraffic` в `AppDependencies`, строка rx/tx в `DebugHarness`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@lib/app/di.dart
@lib/main.dart

<interfaces>
<!-- Уже существует в data-слое фазы 1 (не создавать заново) -->
// lib/features/vpn_connection/domain/repositories/vpn_repository.dart
abstract VpnRepository { Stream<TrafficStats> watchTraffic(); ... }
// lib/features/vpn_connection/domain/entities/traffic_stats.dart
class TrafficStats { final int rxBytes; final int txBytes; }
// VpnRepositoryImpl.watchTraffic() => _ds.traffic  (уже реализовано)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Экспонировать traffic-поток и показать rx/tx в harness</name>
  <read_first>
    - lib/main.dart (DebugHarness: StreamBuilder статуса + список логов; трафика нет)
    - lib/app/di.dart (AppDependencies: собраны usecases; traffic не экспонирован)
    - lib/features/vpn_connection/domain/repositories/vpn_repository.dart (watchTraffic уже есть)
    - lib/features/vpn_connection/domain/entities/traffic_stats.dart
  </read_first>
  <files>lib/app/di.dart, lib/main.dart</files>
  <action>
    В `AppDependencies` (lib/app/di.dart) добавить метод-проброс `Stream<TrafficStats> watchTraffic() => vpnRepository.watchTraffic();` (импорт TrafficStats). Никаких новых репозиториев/датасорсов — используем уже связанный `vpnRepository`.
    В `DebugHarness` (lib/main.dart) добавить `StreamBuilder<TrafficStats>` на `widget.dependencies.watchTraffic()`, отображающий строку вида `rx: <rxBytes> B  tx: <txBytes> B` под блоком статуса (до списка логов). Начальное состояние (нет данных) показывать нулями/прочерком.
    Границы: не добавлять Bloc, не менять DESIGN-экран, не трогать VpnConfig демо-конфиг. Без комментариев (CONVENTIONS.md). `flutter analyze` должен остаться зелёным (very_good_analysis).
  </action>
  <verify><automated>flutter analyze && flutter test && flutter build apk --debug</automated></verify>
  <acceptance_criteria>
    - `grep -c "watchTraffic" lib/app/di.dart` >= 1
    - `grep -c "TrafficStats" lib/main.dart` >= 1
    - `DebugHarness` содержит `StreamBuilder<TrafficStats>` c readout rx/tx
    - `flutter analyze` — No issues found
    - `flutter test` — зелёный (регресс фазы 1)
    - `flutter build apk --debug` собирается
  </acceptance_criteria>
  <done>Harness показывает живые rx/tx; при ping в подсеть туннеля (план 06) значение rx наблюдаемо растёт раз в секунду.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native trafficChanged → Dart harness | счётчики отображаются, доверенный внутрипроцессный поток |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Information Disclosure | Readout в harness | accept | Отображаются только агрегированные счётчики байтов, без creds/UUID; harness — debug-инструмент |
</threat_model>

<verification>
- `flutter analyze && flutter test` — Dart-регресс зелёный
- `flutter build apk --debug` — сборка проходит
- Рост rx при `adb shell ping` — план 06 на эмуляторе
</verification>

<success_criteria>
Debug-harness отображает живые счётчики rx/tx, что делает AND-05 наблюдаемым на phase-gate без построения Phase 3 UI.
</success_criteria>

<output>
Create `.planning/phases/02-android-vpnservice/02-05-SUMMARY.md` when done
</output>
