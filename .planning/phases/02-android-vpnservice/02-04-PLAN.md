---
phase: 02-android-vpnservice
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnConsentGateway.kt
  - android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnHostApiImpl.kt
  - android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt
  - android/app/src/main/res/values/styles.xml
autonomous: true
requirements: [AND-01, AND-03, AND-06]

must_haves:
  truths:
    - "VpnService.prepare() вызывается на КАЖДЫЙ Connect; consent=null → сразу старт сервиса"
    - "RESULT_OK ведёт к startForegroundService(ACTION_CONNECT + config extras)"
    - "Отказ consent (RESULT_CANCELED) → статус Error + внятная строка в логах"
    - "На API 33+ запрашивается POST_NOTIFICATIONS, отказ не блокирует старт VPN"
    - "VpnHostApiImpl делегирует старт/стоп в VpnConsentGateway, getStatus отдаёт snapshot шины"
  artifacts:
    - path: "android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnConsentGateway.kt"
      provides: "Интерфейс абстракции consent/старта (SOLID: сервисный слой не знает Activity)"
      contains: "interface VpnConsentGateway"
    - path: "android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnHostApiImpl.kt"
      provides: "Тонкий адаптер: delegate → gateway, getStatus → snapshot"
      contains: "VpnConsentGateway"
    - path: "android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt"
      provides: "FlutterFragmentActivity: два launcher'а, prepare(), старт/стоп сервиса"
      contains: "FlutterFragmentActivity"
  key_links:
    - from: "MainActivity.kt"
      to: "vpn/OkoVpnService (ACTION_CONNECT + EXTRA_*)"
      via: "startForegroundService(Intent.setAction(ACTION_CONNECT).putExtra(...))"
      pattern: "startForegroundService"
    - from: "VpnHostApiImpl.kt"
      to: "bridge/VpnConsentGateway"
      via: "gateway.connect(config) / gateway.disconnect()"
      pattern: "gateway\\.(connect|disconnect)"
    - from: "MainActivity.kt"
      to: "android VpnService.prepare"
      via: "VpnService.prepare(this)"
      pattern: "VpnService\\.prepare"
---

<objective>
Замкнуть команду Connect на реальный сервис через системный consent. Меняем базовый класс `MainActivity` на `FlutterFragmentActivity` (иначе `registerForActivityResult` не резолвится — Pitfall 1), вводим `registerForActivityResult`-launcher'ы для VPN-consent и POST_NOTIFICATIONS, вызываем `VpnService.prepare()` на каждый Connect и стартуем `OkoVpnService`. `VpnHostApiImpl` вырезает echo-цепочку и делегирует в интерфейс `VpnConsentGateway` (SOLID: bridge-слой не тянет Activity напрямую).

Purpose: подключить consent-флоу и Pigeon-делегирование к сервису из плана 03.
Output: новый `VpnConsentGateway.kt`, переписанный `VpnHostApiImpl.kt`, `MainActivity.kt` на FlutterFragmentActivity.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-android-vpnservice/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt
@android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnHostApiImpl.kt

<interfaces>
<!-- Из плана 03 (OkoVpnService companion) -->
OkoVpnService.ACTION_CONNECT / ACTION_DISCONNECT / EXTRA_HOST / EXTRA_PORT / EXTRA_USER_ID / EXTRA_SERVER_NAME
// Pigeon (сняты в фазе 1):
interface VpnHostApi { fun startVpn(config: VpnConfigMessage, callback: (Result<Unit>) -> Unit); fun stopVpn(callback); fun getStatus(): VpnStatusSnapshotMessage }
VpnHostApi.setUp(binaryMessenger, api)   // companion
VpnEventsStreamHandler.register(messenger, streamHandler)
VpnConfigMessage(host: String, port: Long, userId: String, serverName: String)  // port — Long → putExtra Long
VpnEventBus.emit(...) ; VpnEventBus.snapshot
StatusChangedMessage(status, connectedSinceEpochMs?); LogMessage(text, timestampMillis, level); ErrorMessage(code, message)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VpnConsentGateway + тонкий VpnHostApiImpl</name>
  <read_first>
    - android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnHostApiImpl.kt (текущая echo-реализация — цепочка удаляется)
    - 02-RESEARCH.md → Pattern 1 (тонкий VpnHostApiImpl), Architectural Responsibility Map
  </read_first>
  <files>android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnConsentGateway.kt, android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnHostApiImpl.kt</files>
  <action>
    Создать `interface VpnConsentGateway` в пакете `com.example.vpn_oko.bridge` с методами `fun connect(config: VpnConfigMessage)` и `fun disconnect()` (контракт-первым: MainActivity реализует, VpnHostApiImpl потребляет).
    Переписать `VpnHostApiImpl` в конструктор `VpnHostApiImpl(private val gateway: VpnConsentGateway) : VpnHostApi`:
      `startVpn(config, callback)` → `gateway.connect(config); callback(Result.success(Unit))` (fire-and-forget: прогресс идёт событиями; совместимо с Dart ConnectVpn — echo фазы 1 вёл себя так же).
      `stopVpn(callback)` → `gateway.disconnect(); callback(Result.success(Unit))`.
      `getStatus()` → `VpnEventBus.snapshot` (без изменений).
    Всю синтетическую echo-эмиссию (LogMessage "starting"/"tunnel up", CONNECTING/CONNECTED) УДАЛИТЬ — источником статусов теперь сервис. Без комментариев.
  </action>
  <verify><automated>flutter build apk --debug</automated></verify>
  <acceptance_criteria>
    - `grep -c "interface VpnConsentGateway" android/app/src/main/kotlin/com/example/vpn_oko/bridge/VpnConsentGateway.kt` == 1
    - `VpnHostApiImpl` принимает `VpnConsentGateway` в конструкторе и делегирует connect/disconnect
    - echo-строки "starting"/"tunnel up" в VpnHostApiImpl отсутствуют (`grep -c "tunnel up" ... == 0`)
    - `getStatus()` возвращает `VpnEventBus.snapshot`
    - `flutter build apk --debug` собирается
  </acceptance_criteria>
  <done>Bridge-слой делегирует consent/старт через абстракцию; echo-логика вырезана.</done>
</task>

<task type="auto">
  <name>Task 2: MainActivity → FlutterFragmentActivity + consent-флоу</name>
  <read_first>
    - android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt (текущий FlutterActivity + configureFlutterEngine)
    - android/app/src/main/res/values/styles.xml (темы LaunchTheme/NormalTheme — контроль A3)
    - 02-RESEARCH.md → Pattern 1 (полный consent-флоу), Pitfall 1/4, Assumptions A3/A5
  </read_first>
  <files>android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt, android/app/src/main/res/values/styles.xml</files>
  <action>
    Сменить базовый класс на `io.flutter.embedding.android.FlutterFragmentActivity` и объявить `MainActivity : FlutterFragmentActivity(), VpnConsentGateway`.
    Поле `pendingConfig: VpnConfigMessage?`.
    Зарегистрировать как поля два launcher'а:
      `vpnConsent = registerForActivityResult(ActivityResultContracts.StartActivityForResult()) { result -> ... }`: при `RESULT_OK` и непустом pendingConfig → `startVpnService(config)`; иначе (RESULT_CANCELED) → эмитить в шину `LogMessage("VPN permission denied", now, "error")` (внятная строка в логах, AND-01), `ErrorMessage("consent_denied", "VPN permission denied by user")` и `StatusChangedMessage(status = VpnStatusMessage.ERROR)` — финальный статус Error per Roadmap SC#1/AND-01 (НЕ Disconnected). Обнулять pendingConfig.
      `notifPermission = registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted -> при отказе — LogMessage("notifications denied", now, "warning"); старт VPN не блокируется }`.
    `override fun connect(config: VpnConfigMessage)`: (API33+) запросить POST_NOTIFICATIONS через `notifPermission.launch(...)` если ещё не выдан (единственная развилка `Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU`); затем `val consent = VpnService.prepare(this)`; если `consent == null` → `startVpnService(config)`; иначе `pendingConfig = config; vpnConsent.launch(consent)`. prepare() зовётся на КАЖДЫЙ connect (не кэшировать — Pitfall/Pattern 1).
    `override fun disconnect()`: `startService(Intent(this, OkoVpnService::class.java).setAction(OkoVpnService.ACTION_DISCONNECT))`.
    `private fun startVpnService(config)`: собрать `Intent(this, OkoVpnService::class.java).setAction(ACTION_CONNECT)` с extras (`EXTRA_PORT` через `putExtra(key, config.port)` — Long); `startForegroundService(intent)`.
    В `configureFlutterEngine`: `VpnHostApi.setUp(messenger, VpnHostApiImpl(this))` (this = VpnConsentGateway) + `VpnEventsStreamHandler.register(messenger, VpnEventListener())` (регистрация сохраняется).
    Контингенция A3: если после смены базового класса приложение не запускается из-за темы — задать родителем `LaunchTheme`/`NormalTheme` AppCompat/Material-тему в styles.xml. Если запуск не ломается — styles.xml не трогать. Без комментариев в коде.
  </action>
  <verify><automated>flutter build apk --debug</automated></verify>
  <acceptance_criteria>
    - `grep -c "FlutterFragmentActivity" android/app/src/main/kotlin/com/example/vpn_oko/MainActivity.kt` >= 1
    - `MainActivity` реализует `VpnConsentGateway` (override connect/disconnect)
    - `VpnService.prepare(this)` вызывается в connect(); два `registerForActivityResult` объявлены
    - RESULT_CANCELED-ветка эмитит `StatusChangedMessage(... ERROR)` + error-level LogMessage
    - Развилка POST_NOTIFICATIONS привязана к `Build.VERSION_CODES.TIRAMISU`
    - `startForegroundService` вызывается с ACTION_CONNECT и extras
    - `flutter build apk --debug` собирается
  </acceptance_criteria>
  <done>Тап Connect проходит prepare() каждый раз; согласие стартует foreground-сервис, отказ даёт статус Error с логом; POST_NOTIFICATIONS запрашивается на 13+ и не блокирует старт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter (Dart) → Kotlin (Pigeon HostApi) | команды startVpn/stopVpn пересекают мост |
| Activity → System VPN consent | prepare()/ActivityResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Spoofing / EoP | Старт VPN без согласия пользователя | mitigate | VpnService.prepare() на каждый Connect; establish() без consent вернёт null (обработано в плане 03) |
| T-2-05 | Information Disclosure | userId/UUID в логах при старте | mitigate | В LogMessage не попадают creds; логируются только переходы/причины |
| T-2-SC | Tampering | package installs | accept | Фаза не ставит внешних пакетов; androidx.activity/core приходят транзитивно с Flutter embedding |
</threat_model>

<verification>
- `flutter build apk --debug` — Kotlin компилируется, смена базового класса не ломает сборку
- Реальный consent-диалог, RESULT_OK→Connected, Cancel→Error, запуск приложения (тема FlutterFragmentActivity, A3) — план 06 на эмуляторе
</verification>

<success_criteria>
Connect из Flutter проходит системный consent на каждый вызов; согласие поднимает реальный сервис, отказ доводит статус Error с внятным логом; POST_NOTIFICATIONS запрашивается на Android 13+; VpnHostApiImpl — тонкий адаптер над VpnConsentGateway.
</success_criteria>

<output>
Create `.planning/phases/02-android-vpnservice/02-04-SUMMARY.md` when done
</output>
