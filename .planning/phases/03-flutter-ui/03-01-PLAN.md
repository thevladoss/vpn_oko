---
phase: 03-flutter-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - google_fonts/
autonomous: false
requirements: [UI-06, QA-02]
user_setup: []

must_haves:
  truths:
    - "flutter pub get resolves with google_fonts and bloc_test present"
    - "Space Grotesk, Inter, JetBrains Mono доступны приложению офлайн ИЛИ задокументирован online-first фолбэк"
  artifacts:
    - path: "pubspec.yaml"
      provides: "google_fonts runtime dep + bloc_test dev dep + assets google_fonts/"
      contains: "google_fonts"
    - path: "google_fonts/"
      provides: "бандл трёх семейств шрифтов для офлайн (Pitfall 5)"
  key_links:
    - from: "pubspec.yaml"
      to: "google_fonts package"
      via: "dependencies"
      pattern: "google_fonts:"
    - from: "pubspec.yaml"
      to: "bloc_test package"
      via: "dev_dependencies"
      pattern: "bloc_test:"
---

<objective>
Установить две новые pub-зависимости фазы после человеко-верификации их легитимности и подготовить офлайн-бандл шрифтов. Это единственный шаг фазы с package-install; всё остальное строится на нём.

Purpose: `google_fonts` даёт Space Grotesk / Inter / JetBrains Mono для типографики (UI-06), `bloc_test` — фреймворк для Bloc-тестов (QA-02). Обе помечены `[ASSUMED]` в 03-RESEARCH.md (slopcheck был недоступен), поэтому установка проходит через блокирующий checkpoint легитимности пакетов.
Output: обновлённый `pubspec.yaml`, папка `google_fonts/` с .ttf, зелёный `flutter pub get`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — google_fonts + bloc_test</name>
  <files></files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (## Package Legitimacy Audit, Pitfall 5)
  </read_first>
  <action>Приостановить исполнение и показать пользователю верификацию легитимности google_fonts и bloc_test. НЕ выполнять `flutter pub add` до подтверждения. Checkpoint блокирующий и не авто-одобряется (package legitimacy gate игнорирует auto_advance).</action>
  <what-built>
    Планировщик уже проверил обе зависимости на pub.dev:
    - google_fonts 8.1.0 — verified publisher flutter.dev (команда Flutter), поддерживает бандлинг шрифтов ассетами
    - bloc_test 10.0.0 — verified publisher bloclibrary.dev (автор felangel), зависит от bloc ^9.0.0 (совместимо с flutter_bloc 9.1.1 проекта)
    Оба помечены `[ASSUMED]` в research только потому, что slopcheck-инструмент был недоступен в окружении; их существование, версии и издатели подтверждены на официальном pub.dev.
  </what-built>
  <how-to-verify>
    1. Открыть https://pub.dev/packages/google_fonts — убедиться: publisher flutter.dev (значок verified), версия 8.x.
    2. Открыть https://pub.dev/packages/bloc_test — убедиться: publisher bloclibrary.dev (значок verified), версия 10.x, dependency `bloc: ^9.0.0`.
    3. Подтвердить, что установка обоих пакетов разрешена.
  </how-to-verify>
  <acceptance_criteria>
    - Пользователь подтвердил обе зависимости словом "approved" (или указал замену/отказ).
    - Checkpoint не авто-одобряется (package legitimacy gate игнорирует auto_advance).
  </acceptance_criteria>
  <resume-signal>Напишите "approved" для установки обоих пакетов, либо опишите возражение.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Установить пакеты и забандлить шрифты</name>
  <files>pubspec.yaml, google_fonts/</files>
  <read_first>
    - pubspec.yaml (текущие dependencies/dev_dependencies)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pitfall 5: офлайн-бандлинг google_fonts, Installation)
  </read_first>
  <action>
    Выполнить `flutter pub add google_fonts` и `flutter pub add --dev bloc_test` из корня проекта. Проверить, что pub подобрал google_fonts ^8.1.0 и bloc_test ^10.0.0 (совместим с bloc 9.x).

    Забандлить шрифты для офлайн-надёжности демо (Pitfall 5): создать папку `google_fonts/` в корне проекта, положить туда .ttf нужных начертаний трёх семейств — Space Grotesk (Regular 400, SemiBold 600), Inter (Regular 400, SemiBold 600), JetBrains Mono (Regular 400). Именование файлов — как в репозитории Google Fonts (например `SpaceGrotesk-SemiBold.ttf`), google_fonts матчит бандл по имени семейства/начертания и приоритизирует ассет над HTTP-загрузкой. Объявить в `pubspec.yaml` под `flutter: assets:` путь `google_fonts/`.

    Источник .ttf — официальный репозиторий шрифтов Google Fonts. Если в окружении нет сети для скачивания .ttf, НЕ блокировать план: оставить папку `google_fonts/` пустой или отсутствующей, задокументировать в SUMMARY, что применяется online-first фолбэк (google_fonts тянет шрифты при первом запуске с сетью), и что для офлайн-демо файлы нужно доложить вручную. Хардкод путей к шрифтам и ручной FontLoader запрещены — только google_fonts API.
  </action>
  <verify>
    <automated>flutter pub get && grep -q 'google_fonts:' pubspec.yaml && grep -q 'bloc_test:' pubspec.yaml && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` завершается без ошибок разрешения версий.
    - `pubspec.yaml` содержит `google_fonts:` в `dependencies` и `bloc_test:` в `dev_dependencies`.
    - Если сеть доступна: `google_fonts/` содержит .ttf трёх семейств и объявлена в `flutter: assets:`. Если сети нет: online-first фолбэк задокументирован в SUMMARY.
    - `flutter analyze` без новых варнингов.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → проект | Package installs — единственная точка supply-chain-риска фазы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering | `flutter pub add google_fonts`, `flutter pub add --dev bloc_test` | mitigate | Блокирующий human-verify checkpoint (Task 1) до install; обе — verified publishers (flutter.dev, bloclibrary.dev); checkpoint не авто-одобряется |
</threat_model>

<verification>
`flutter pub get` зелёный; `flutter analyze` без варнингов; обе зависимости присутствуют в pubspec.
</verification>

<success_criteria>
google_fonts и bloc_test установлены после подтверждения легитимности; шрифты забандлены (или задокументирован фолбэк); проект резолвится и анализируется без ошибок.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-01-SUMMARY.md` по завершении.
</output>
