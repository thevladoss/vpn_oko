---
phase: 03-flutter-ui
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - lib/core/theme/vpn_status.dart
  - lib/core/theme/oko_tones.dart
  - lib/core/theme/oko_typography.dart
  - lib/core/theme/oko_theme.dart
  - lib/core/theme/oko_motion.dart
  - test/core/theme/oko_tones_test.dart
autonomous: true
requirements: [UI-06]

must_haves:
  truths:
    - "Обе темы (dark сигнатурная + light) регистрируют OkoTones и читаются через Theme.of(context).extension<OkoTones>()"
    - "accentFor(VpnStatus) возвращает правильный акцент для каждого из пяти статусов"
    - "Типографика собрана на google_fonts (Space Grotesk / Inter / JetBrains Mono) с табличными цифрами на display"
  artifacts:
    - path: "lib/core/theme/vpn_status.dart"
      provides: "enum VpnStatus — общий словарь статусов для темы, Bloc и виджетов"
      contains: "enum VpnStatus"
    - path: "lib/core/theme/oko_tones.dart"
      provides: "ThemeExtension<OkoTones> с accentFor/lerp/copyWith + dark/light константы"
      contains: "class OkoTones extends ThemeExtension"
    - path: "lib/core/theme/oko_theme.dart"
      provides: "OkoTheme.dark / OkoTheme.light -> ThemeData с ColorScheme + extensions"
      contains: "class OkoTheme"
    - path: "lib/core/theme/oko_typography.dart"
      provides: "TextTheme на google_fonts, 4 кегля / 2 начертания"
    - path: "lib/core/theme/oko_motion.dart"
      provides: "const длительности и кривые motion"
  key_links:
    - from: "lib/core/theme/oko_theme.dart"
      to: "OkoTones"
      via: "ThemeData.extensions"
      pattern: "extensions:.*OkoTones"
    - from: "lib/core/theme/oko_tones.dart"
      to: "VpnStatus"
      via: "accentFor switch"
      pattern: "accentFor"
---

<objective>
Собрать дизайн-систему фазы в `lib/core/theme/`: цветовые токены (OkoTones), типографику, обе ThemeData и motion-константы. Это фундамент, от которого зависят все виджеты.

Purpose: UI-06 требует обе темы через `ThemeExtension` (не хардкод цветов в виджетах), фирменную палитру состояний и единый ритм motion. Виджеты берут акцент состояния через `OkoTones.accentFor(VpnStatus)` — единственный источник маппинга статус→цвет, никаких switch по хардкоду в виджетах.
Output: пять файлов `lib/core/theme/*` + тест токенов.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<architecture_note>
VpnStatus живёт в `lib/core/theme/vpn_status.dart` (не в feature-слое), потому что `OkoTones.accentFor(VpnStatus)` — метод темы (core) и нуждается в этом enum. Bloc и виджеты импортируют VpnStatus из core (feature → core, законный слой). Значения enum: disconnected, connecting, connected, disconnecting, error — один-к-одному с подтипами domain `VpnState`.
</architecture_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VpnStatus + OkoTones + OkoMotion</name>
  <files>lib/core/theme/vpn_status.dart, lib/core/theme/oko_tones.dart, lib/core/theme/oko_motion.dart, test/core/theme/oko_tones_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (## Color — таблицы dark/light; ## Motion)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 5: ThemeExtension<OkoTones> с lerp)
  </read_first>
  <behavior>
    - accentFor(connected) == accentConnected; accentFor(connecting)==accentFor(disconnecting)==accentTransitional; accentFor(error)==accentError; accentFor(disconnected)==accentIdle
    - lerp(OkoTones.dark, OkoTones.light, 0.0) равен dark по каждому полю; lerp(..., 1.0) равен light
    - OkoTones.dark и OkoTones.light — const, все поля из UI-SPEC заполнены
  </behavior>
  <action>
    Создать `vpn_status.dart`: `enum VpnStatus { disconnected, connecting, connected, disconnecting, error }`.

    Создать `oko_tones.dart`: `@immutable class OkoTones extends ThemeExtension<OkoTones>` с полями-Color: accentConnected, accentTransitional, accentError, accentIdle, glow, surfaceCard, surfaceElevated, textPrimary, textSecondary. Метод `Color accentFor(VpnStatus status)` через exhaustive switch (connected→accentConnected; connecting||disconnecting→accentTransitional; error→accentError; disconnected→accentIdle). Реализовать `copyWith` (все поля опциональны) и `lerp` через `Color.lerp` по каждому полю (при other не OkoTones вернуть this). Определить `static const dark` и `static const light` со значениями из UI-SPEC ## Color: dark accentConnected `#2CE5A7`, transitional `#FFB454`, error `#FF5C5C`, idle `#5A6472`, glow берётся от акцента состояния на уровне виджета (в токене хранить базовый glow-цвет surface-независимо — использовать белый/акцентный низкой альфы; конкретную альфу glow задаёт виджет ириса); surfaceCard `#121821`, surfaceElevated `#1A2230`, textPrimary `#F2F5F9`, textSecondary `#8B95A5`. light: фон/surfaces белые (#FFFFFF card / #EEF1F5 elevated), textPrimary `#0B0F14`, textSecondary `#5A6472`, accentConnected `#0E9E75`, transitional `#B26A00`, error `#C62828`, idle `#5A6472` (контраст ≥4.5:1). Хардкод Color(0xFF...) допустим ТОЛЬКО здесь (единственный источник токенов); в виджетах он запрещён.

    Создать `oko_motion.dart`: класс `OkoMotion` с `static const Duration` и кривыми из UI-SPEC ## Motion — enterScreen 350ms easeOutCubic (+ задержки stagger iris 0 / serverCard 60 / trafficPanel 120 / connectButton 180 / logConsole 240 мс), statusCrossfade 300ms easeInOut, pupilOpen 600ms easeOutBack, glowBreath 4000ms, segment 1200ms linear, ringBreath 1600ms, shake 250ms, autoscroll 200ms easeOut. Значения — const, единый источник ритма.

    Написать `test/core/theme/oko_tones_test.dart` по `<behavior>` (accentFor маппинг всех пяти статусов, lerp на границах 0.0/1.0). Комментарии в коде запрещены.
  </action>
  <verify>
    <automated>flutter test test/core/theme/oko_tones_test.dart && flutter analyze lib/core/theme/vpn_status.dart lib/core/theme/oko_tones.dart lib/core/theme/oko_motion.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/theme/oko_tones_test.dart` зелёный (accentFor для 5 статусов, lerp границы).
    - OkoTones реализует copyWith и lerp по всем полям; dark и light — const с полным набором из UI-SPEC.
    - OkoMotion содержит все длительности/кривые/задержки из ## Motion как const.
    - `flutter analyze` без варнингов; ноль комментариев в коде.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Типографика и обе ThemeData</name>
  <files>lib/core/theme/oko_typography.dart, lib/core/theme/oko_theme.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (## Typography; ## Design System — themeMode.system, явный ColorScheme не fromSeed)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Code Examples: Tabular figures в TextTheme через google_fonts)
    - lib/core/theme/oko_tones.dart (созданный в Task 1 — регистрируется как extension)
  </read_first>
  <action>
    Создать `oko_typography.dart`: функция(и), собирающие `TextTheme` на `google_fonts` для заданной `Brightness`. Ровно 4 кегля (48 display/timer, 20 title, 15 body, 12 label/caption/mono) и 2 начертания (w400, w600). Space Grotesk — displayLarge (48/600, height 1.0) и titleMedium (20/600); Inter — bodyMedium (15/400), labelSmall (12/600 uppercase-роль), bodySmall/caption (12/400); JetBrains Mono — отдельный стиль для строк логов (12/400). На display/timer и на числах трафика применить `fontFeatures: [FontFeature.tabularFigures()]`. Цвета текста берутся из ColorScheme/OkoTones темы, не хардкодом здесь.

    Создать `oko_theme.dart`: класс `OkoTheme` с `static ThemeData get dark` и `static ThemeData get light`. Каждая: `useMaterial3: true`, явный `ColorScheme` (НЕ `ColorScheme.fromSeed`) из палитры UI-SPEC (dark — Brightness.dark, surface #0B0F14 / #121821, onSurface #F2F5F9; light аналогично из светлой палитры), `textTheme` из oko_typography для соответствующей Brightness, и `extensions: [OkoTones.dark]` / `[OkoTones.light]`. Кнопки/скругления по умолчанию не переопределять глобально сверх необходимого (стили ведут сами виджеты). Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter analyze lib/core/theme/oko_typography.dart lib/core/theme/oko_theme.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` по обоим файлам без варнингов.
    - OkoTheme.dark и OkoTheme.light возвращают ThemeData с `useMaterial3: true`, явным ColorScheme (не fromSeed), textTheme на google_fonts и `extensions` с OkoTones соответствующего режима.
    - display/timer и числа трафика используют FontFeature.tabularFigures.
    - Ноль комментариев в коде.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет | План — чистая презентация (темы/токены); недоверенный ввод не пересекает границ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | oko_theme / oko_typography | accept | Темы не обрабатывают данные и не пересекают доверительную границу; риск отсутствует |
</threat_model>

<verification>
`flutter test test/core/theme/oko_tones_test.dart` зелёный; `flutter analyze lib/core/theme/` без варнингов; обе темы регистрируют OkoTones.
</verification>

<success_criteria>
Дизайн-система собрана: VpnStatus, OkoTones (accentFor/lerp, dark+light), типографика на google_fonts, обе ThemeData, motion-константы. Виджеты могут читать токены через Theme.of(context).extension<OkoTones>().
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-02-SUMMARY.md` по завершении.
</output>
