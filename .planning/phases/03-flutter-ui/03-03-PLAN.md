---
phase: 03-flutter-ui
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - lib/features/vpn_connection/domain/usecases/watch_traffic.dart
  - lib/features/vpn_connection/presentation/bloc/vpn_connection_event.dart
  - lib/features/vpn_connection/presentation/bloc/vpn_connection_state.dart
  - lib/features/vpn_connection/presentation/bloc/vpn_connection_bloc.dart
  - test/features/vpn_connection/presentation/bloc/vpn_connection_bloc_test.dart
  - test/helpers/mock_vpn_usecases.dart
autonomous: true
requirements: [UI-01, UI-02, UI-05, UI-07, QA-02]

must_haves:
  truths:
    - "VpnStarted вызывает syncStatus() ровно раз, затем подписывается на watchVpnState и watchTraffic через internal-события"
    - "Стрим VpnState маппится в VpnConnectionState с правильным status, connectedSince на Connected, errorMessage на Error"
    - "ConnectRequested/DisconnectRequested заблокированы гейтом isBusy в переходных статусах (двойной тап не проходит)"
    - "Системный VpnDisconnected из стрима переводит в disconnected без вызова disconnectVpn (onRevoke)"
  artifacts:
    - path: "lib/features/vpn_connection/presentation/bloc/vpn_connection_bloc.dart"
      provides: "VpnConnectionBloc — статус-машина UI поверх usecases"
      contains: "class VpnConnectionBloc"
      min_lines: 40
    - path: "lib/features/vpn_connection/presentation/bloc/vpn_connection_state.dart"
      provides: "VpnConnectionState (status/connectedSince/rx/tx/errorMessage) + isBusy + copyWith"
      contains: "class VpnConnectionState extends Equatable"
    - path: "lib/features/vpn_connection/presentation/bloc/vpn_connection_event.dart"
      provides: "sealed события VpnStarted/VpnStateReceived/VpnTrafficReceived/ConnectRequested/DisconnectRequested"
      contains: "sealed class VpnConnectionEvent"
    - path: "lib/features/vpn_connection/domain/usecases/watch_traffic.dart"
      provides: "WatchTraffic usecase-обёртка над vpnRepository.watchTraffic()"
      contains: "class WatchTraffic"
    - path: "test/features/vpn_connection/presentation/bloc/vpn_connection_bloc_test.dart"
      provides: "6 сценариев QA-02"
      contains: "blocTest"
  key_links:
    - from: "vpn_connection_bloc.dart"
      to: "watchVpnState().listen"
      via: "internal-event add(VpnStateReceived)"
      pattern: "listen\\(.*VpnStateReceived"
    - from: "vpn_connection_bloc.dart"
      to: "syncStatus"
      via: "await в _onStarted до подписки"
      pattern: "syncStatus"
---

<objective>
Построить `VpnConnectionBloc` — статус-машину presentation-слоя поверх готовых usecases, и покрыть её шестью unit-сценариями (QA-02). Bloc — источник производного UI-state (status, connectedSince, rx/tx, errorMessage), который экран раздаёт виджетам.

Purpose: UI-01 (пять статусов), UI-02 (гейт двойного тапа), UI-05 (rx/tx в state), UI-07 (восстановление через syncStatus), QA-02 (тесты переходов, error, onRevoke, double-tap guard, syncStatus×1). Реализовать подписку на два бесконечных стрима корректным паттерном internal-событий (03-RESEARCH Pattern 1), а НЕ двойным emit.forEach (Pitfall 1: второй emit.forEach никогда не стартует, трафик умрёт).
Output: usecase WatchTraffic + три файла bloc + тест + моки usecases.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Готовые контракты из кодовой базы — использовать напрямую, без исследования. -->

domain VpnState (lib/features/vpn_connection/domain/entities/vpn_state.dart):
  sealed class VpnState; VpnDisconnected(); VpnConnecting();
  VpnConnected({DateTime? connectedSince}); VpnDisconnecting(); VpnError(String message)

TrafficStats (lib/.../domain/entities/traffic_stats.dart): TrafficStats({required int rxBytes, required int txBytes})
VpnConfig (lib/.../domain/entities/vpn_config.dart): VpnConfig({required host, port, userId, serverName})

usecases (call-объекты, конструктор принимает VpnRepository):
  WatchVpnState()      -> Stream<VpnState>   (watch_vpn_state.dart, .call())
  ConnectVpn(config)   -> Future<void>       (connect_vpn.dart)
  DisconnectVpn()      -> Future<void>       (disconnect_vpn.dart)
  SyncStatus()         -> Future<void>       (sync_status.dart)
  VpnRepository.watchTraffic() -> Stream<TrafficStats>  (обернуть в новый WatchTraffic)

VpnStatus enum: lib/core/theme/vpn_status.dart (создан планом 03-02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WatchTraffic usecase + события + state</name>
  <files>lib/features/vpn_connection/domain/usecases/watch_traffic.dart, lib/features/vpn_connection/presentation/bloc/vpn_connection_event.dart, lib/features/vpn_connection/presentation/bloc/vpn_connection_state.dart</files>
  <read_first>
    - lib/features/vpn_connection/domain/usecases/watch_vpn_state.dart (образец usecase для симметрии)
    - lib/features/vpn_connection/domain/repositories/vpn_repository.dart (watchTraffic сигнатура)
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (## Bloc-контракт — таблицы событий и state)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 3: copyWith с флагами clearConnectedSince/clearError)
  </read_first>
  <action>
    Создать `watch_traffic.dart` — usecase `WatchTraffic` по образцу `WatchVpnState`: конструктор принимает `VpnRepository`, `Stream<TrafficStats> call() => _repository.watchTraffic()`.

    Создать `vpn_connection_event.dart` — `sealed class VpnConnectionEvent extends Equatable`, подтипы: `VpnStarted`; `VpnStateReceived(VpnState state)`; `VpnTrafficReceived(TrafficStats stats)`; `ConnectRequested`; `DisconnectRequested`. props у несущих данные событий включают их поля.

    Создать `vpn_connection_state.dart` — `class VpnConnectionState extends Equatable` с полями: `VpnStatus status`, `DateTime? connectedSince`, `int rxBytes` (=0), `int txBytes` (=0), `String? errorMessage`. Производный геттер `bool get isBusy => status == connecting || status == disconnecting`. `copyWith` с явными флагами `bool clearConnectedSince = false` и `bool clearError = false` (обнуление nullable без потери «не передано» — Pattern 3). Начальное состояние конструируется как `VpnConnectionState(status: VpnStatus.disconnected)`. props перечисляет все поля. Импорт VpnStatus из `lib/core/theme/vpn_status.dart`. Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter analyze lib/features/vpn_connection/domain/usecases/watch_traffic.dart lib/features/vpn_connection/presentation/bloc/vpn_connection_event.dart lib/features/vpn_connection/presentation/bloc/vpn_connection_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - Три файла компилируются, `flutter analyze` без варнингов.
    - VpnConnectionState: isBusy true только для connecting/disconnecting; copyWith умеет обнулять connectedSince и errorMessage через флаги.
    - События — sealed, с корректными props.
    - Ноль комментариев в коде.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VpnConnectionBloc + 6 сценариев QA-02</name>
  <files>lib/features/vpn_connection/presentation/bloc/vpn_connection_bloc.dart, test/features/vpn_connection/presentation/bloc/vpn_connection_bloc_test.dart, test/helpers/mock_vpn_usecases.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 1: internal-events; Pitfall 1, 2, 8; Code Examples: blocTest для QA-02)
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### QA-02 — 6 сценариев; маппинг VpnState→VpnStatus)
    - test/features/vpn_connection/data/repositories/vpn_repository_impl_test.dart (образец mocktail/fake стиля проекта)
    - lib/features/vpn_connection/presentation/bloc/vpn_connection_state.dart (созданный в Task 1)
  </read_first>
  <behavior>
    - Сценарий 1 (UI-01): watchVpnState эмитит VpnConnecting затем VpnConnected(connectedSince) → две VpnConnectionState со status connecting затем connected + connectedSince проброшен
    - Сценарий 2: seed connected; DisconnectRequested → disconnectVpn вызван 1 раз; при приходе VpnDisconnected connectedSince обнулён (clearConnectedSince)
    - Сценарий 3 (QA-02 error): VpnError('msg') → status error, errorMessage 'msg'; при выходе из error errorMessage обнуляется
    - Сценарий 4 (onRevoke): в connected стрим приносит VpnDisconnected без DisconnectRequested → status disconnected, verifyNever(disconnectVpn)
    - Сценарий 5 (double-tap guard, UI-02): seed connecting; два ConnectRequested → verifyNever(connectVpn); seed disconnecting; два DisconnectRequested → verifyNever(disconnectVpn)
    - Сценарий 6 (UI-07): на VpnStarted syncStatus вызван ровно 1 раз
  </behavior>
  <action>
    Написать сперва тест `vpn_connection_bloc_test.dart` (RED) по `<behavior>` через `blocTest<VpnConnectionBloc, VpnConnectionState>` (пакет bloc_test) + mocktail-моки. Создать `test/helpers/mock_vpn_usecases.dart` с `Mock`-классами для WatchVpnState, WatchTraffic, ConnectVpn, DisconnectVpn, SyncStatus (mocktail). Для стримов использовать `Stream.fromIterable`/`StreamController`, который тест закрывает; где стрим живёт дольше act — `wait`. Зарегистрировать fallback-значение VpnConfig через `registerFallbackValue`, если требуется для `any()`.

    Затем реализовать `vpn_connection_bloc.dart` (GREEN): `class VpnConnectionBloc extends Bloc<VpnConnectionEvent, VpnConnectionState>`. Конструктор инъектит `WatchVpnState, WatchTraffic, ConnectVpn, DisconnectVpn, SyncStatus, VpnConfig config`; `super(const VpnConnectionState(status: VpnStatus.disconnected))`; регистрирует все `on<...>`. Обработчик `_onStarted`: `await syncStatus()` в try/catch (при Failure — emit status error), ЗАТЕМ `_stateSub = watchVpnState().listen((s) => add(VpnStateReceived(s)))` и `_trafficSub = watchTraffic().listen((t) => add(VpnTrafficReceived(t)))`. НЕ использовать два emit.forEach подряд (Pitfall 1). `_onStateReceived` маппит domain VpnState в VpnConnectionState: VpnDisconnected→disconnected(clearConnectedSince, clearError при выходе из error), VpnConnecting→connecting, VpnConnected→connected(connectedSince), VpnDisconnecting→disconnecting, VpnError→error(errorMessage). `_onTrafficReceived` обновляет rxBytes/txBytes. `_onConnectRequested`/`_onDisconnectRequested`: `if (state.isBusy) return;` затем fire-and-forget usecase. Переопределить `close()`: отменить обе StreamSubscription. Хранить подписки полями `StreamSubscription<...>?`. Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/bloc/vpn_connection_bloc_test.dart && flutter analyze lib/features/vpn_connection/presentation/bloc/vpn_connection_bloc.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../vpn_connection_bloc_test.dart` зелёный — все 6 сценариев QA-02 проходят.
    - syncStatus вызывается ровно раз на VpnStarted (verify count 1); подписка идёт после await (Pitfall 8).
    - Гейт isBusy блокирует повторный Connect/Disconnect (verifyNever).
    - onRevoke: VpnDisconnected из стрима не вызывает disconnectVpn.
    - close() отменяет обе подписки; `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native VpnState/TrafficStats → Bloc | Данные из собственного native-сервиса через верифицированный мост; не внешняя сеть |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Denial of Service | подписки на бесконечные стримы | mitigate | StreamSubscription отменяются в close() (нет утечки тикеров/подписок); syncStatus в try/catch не роняет Bloc |
| T-3-04 | Information Disclosure | errorMessage из VpnError | accept | Сообщение — из собственного native (userId уже не логируется, решение 02-03); показывается как текст, не исполняется |
</threat_model>

<verification>
`flutter test test/features/vpn_connection/presentation/bloc/vpn_connection_bloc_test.dart` зелёный; весь `flutter test` остаётся зелёным; `flutter analyze` чист.
</verification>

<success_criteria>
VpnConnectionBloc корректно маппит пять статусов, восстанавливает статус через syncStatus, блокирует двойной тап, обрабатывает onRevoke и error; 6 сценариев QA-02 зелёные; подписки чисто закрываются.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-03-SUMMARY.md` по завершении.
</output>
