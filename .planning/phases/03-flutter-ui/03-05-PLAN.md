---
phase: 03-flutter-ui
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - lib/features/vpn_connection/presentation/widgets/iris_painter.dart
  - lib/features/vpn_connection/presentation/widgets/iris_indicator.dart
  - lib/features/vpn_connection/presentation/widgets/connection_timer.dart
  - test/features/vpn_connection/presentation/widgets/iris_indicator_test.dart
autonomous: true
requirements: [UI-01, UI-04, UI-06]

must_haves:
  truths:
    - "IrisIndicator рисует все пять статусов через один CustomPainter, реагируя на VpnStatus"
    - "Анимации (сегмент, дыхание, раскрыв, shake) отключаются при MediaQuery.disableAnimations — рисуется статичный кадр"
    - "ConnectionTimer тикает от connectedSince и не сбрасывается при пересоздании (источник истины — connectedSince)"
  artifacts:
    - path: "lib/features/vpn_connection/presentation/widgets/iris_painter.dart"
      provides: "CustomPainter геометрии ириса (кольцо/зрачок/glow/сегмент/shake) для 5 статусов"
      contains: "class IrisPainter extends CustomPainter"
    - path: "lib/features/vpn_connection/presentation/widgets/iris_indicator.dart"
      provides: "StatefulWidget + AnimationController + Semantics liveRegion + reduce-motion"
      contains: "class IrisIndicator"
      min_lines: 40
    - path: "lib/features/vpn_connection/presentation/widgets/connection_timer.dart"
      provides: "Timer.periodic от connectedSince, HH:MM:SS, FittedBox, tabular"
      contains: "class ConnectionTimer"
  key_links:
    - from: "iris_indicator.dart"
      to: "OkoTones.accentFor(status)"
      via: "цвет кольца/glow из темы"
      pattern: "accentFor"
    - from: "connection_timer.dart"
      to: "hhmmss"
      via: "форматирование длительности"
      pattern: "hhmmss"
    - from: "iris_indicator.dart"
      to: "MediaQuery.disableAnimationsOf"
      via: "reduce-motion гейт"
      pattern: "disableAnimations"
---

<objective>
Построить сигнатурный элемент экрана — ирис-индикатор (`CustomPainter` + `AnimationController`) на пять статусов и таймер подключения в его центре. Это «hero moment» демо (Connect → зрачок раскрывается → таймер).

Purpose: UI-01 (визуальные пять статусов), UI-06 (кастомная графика, motion, reduce-motion, доступность), UI-04 (таймер от connectedSince, переживает пересоздание). Один painter, вся геометрия параметризована анимируемыми значениями; loop-анимации и overshoot/shake уважают MediaQuery.disableAnimations (Pitfall 3, 7).
Output: iris_painter, iris_indicator, connection_timer + smoke-тест.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md

<interfaces>
VpnStatus: lib/core/theme/vpn_status.dart (disconnected/connecting/connected/disconnecting/error)
OkoTones: Theme.of(context).extension<OkoTones>()! — accentFor(VpnStatus), glow, textPrimary
OkoMotion: lib/core/theme/oko_motion.dart — segment 1200ms, ringBreath 1600ms, glowBreath 4000ms, pupilOpen 600ms easeOutBack, shake 250ms, statusCrossfade 300ms
hhmmss: lib/features/vpn_connection/presentation/formatters/duration_format.dart
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IrisPainter — геометрия пяти статусов</name>
  <files>lib/features/vpn_connection/presentation/widgets/iris_painter.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### IrisIndicator + IrisPainter — таблица кольцо/зрачок/glow/анимация по статусам; геометрия диаметр min*0.8, толщина 3..8)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 6; Pitfall 7: shouldRepaint)
  </read_first>
  <action>
    Создать `iris_painter.dart` — `class IrisPainter extends CustomPainter`. Поля: `VpnStatus status`, анимируемые значения `double segment, breath, pupil, shake`, `Color accent, Color glow`. `paint`: центрированный круг диаметром `min(size.width, size.height) * 0.8`; кольцо `PaintingStyle.stroke` толщиной по статусу (disconnected 3, connecting/disconnecting 5, connected 8); «зрачок» — внутренний круг, радиус ведёт `pupil` (закрыт для disconnected/error, приоткрыт connecting, раскрыт connected). Радиальный glow под кольцом как `RadialGradient` от центра, альфа по статусу (connecting amber 0.10, connected green 0.10..0.16 через breath, нет для disconnected). Connecting — яркий сегмент дуги ~40°, угол ведёт `segment`; кольцо масштабируется `breath` (0.97..1.03). Error — разрыв дуги ~30°, смещение по X на `shake` (sin, ±8px). Цвет кольца/glow берётся из полей accent/glow (переданы из виджета, интерполированы). `shouldRepaint`: true при изменении status/segment/breath/pupil/shake/accent/glow (сравнить все анимируемые поля — иначе ирис замёрзнет, Pitfall 7). Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter analyze lib/features/vpn_connection/presentation/widgets/iris_painter.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` без варнингов.
    - IrisPainter параметризован анимируемыми значениями; shouldRepaint сравнивает все анимируемые поля (не возвращает const false).
    - Геометрия покрывает пять статусов (толщина кольца, состояние зрачка, glow, сегмент, разрыв дуги).
    - Ноль комментариев в коде; цвета только из переданных полей (без хардкода Color).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: IrisIndicator (анимации + reduce-motion + Semantics) и ConnectionTimer</name>
  <files>lib/features/vpn_connection/presentation/widgets/iris_indicator.dart, lib/features/vpn_connection/presentation/widgets/connection_timer.dart, test/features/vpn_connection/presentation/widgets/iris_indicator_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 6: AnimationController+reduce-motion; Pattern 7: ConnectionTimer; Pitfall 3, 4)
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### ConnectionTimer; ## Accessibility — Iris Semantics liveRegion; ## Motion — haptics enter/error)
    - lib/features/vpn_connection/presentation/widgets/iris_painter.dart (Task 1)
  </read_first>
  <behavior>
    - Прогон IrisIndicator для каждого из 5 статусов не бросает исключений (pump + pumpAndSettle)
    - При MediaQuery(disableAnimations: true) виджет строит статичный кадр без бесконечных контроллеров (нет висящих таймеров после pump)
    - ConnectionTimer с connectedSince==null возвращает пустой SizedBox (не тикает)
  </behavior>
  <action>
    Создать `iris_indicator.dart` — `class IrisIndicator extends StatefulWidget` c `final VpnStatus status`, `State` с `TickerProviderStateMixin`. Контроллеры: `_segment` (OkoMotion.segment), `_breath` (ringBreath/glowBreath), `_pupil` (pupilOpen easeOutBack), `_shake` (shake). В `didChangeDependencies`/`didUpdateWidget` при смене статуса вызвать `_applyStatus`: читать `MediaQuery.disableAnimationsOf(context)` (НЕ в initState — Pitfall 3); при reduce — остановить все loop и нарисовать статичный кадр; иначе запустить по статусу (connecting: segment.repeat + breath.repeat reverse; connected: pupil.forward(from:0) + breath.repeat reverse + HapticFeedback.lightImpact; error: shake.forward(from:0) + HapticFeedback.heavyImpact один раз). Цвет кольца/glow интерполировать `ColorTween` за statusCrossfade (300ms) при смене статуса. `build`: `AnimatedBuilder(animation: Listenable.merge([...контроллеры]))` → `CustomPaint(painter: IrisPainter(...значения..., accent: tones.accentFor(status), glow: tones.glow))`. Обернуть в `Semantics(liveRegion: true, label: ...)` по статусу (disconnected 'VPN disconnected', connecting 'Connecting…', connected 'VPN connected', disconnecting 'Disconnecting', error 'Connection error'); центр — `ConnectionTimer` для connected. Диспоузить все контроллеры в `dispose` (Pitfall 3).

    Создать `connection_timer.dart` — `class ConnectionTimer extends StatefulWidget` c `final DateTime? connectedSince`. `Timer.periodic(1s, setState)`; `didUpdateWidget` перезапускает таймер только при смене connectedSince (Pitfall 4). `build`: если connectedSince==null → `SizedBox.shrink()`; иначе `FittedBox(child: Text(hhmmss(DateTime.now().difference(connectedSince))))` стилем display (Space Grotesk 48/600 tabular из TextTheme). Диспоуз таймера. setState здесь допустим только для тика (не бизнес-логика). Комментарии запрещены.

    Написать `iris_indicator_test.dart` (smoke) по `<behavior>`: pump IrisIndicator в MaterialApp(theme: OkoTheme.dark) для каждого статуса; отдельный кейс с `MediaQueryData(disableAnimations: true)`; кейс ConnectionTimer(connectedSince: null) → SizedBox.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/widgets/iris_indicator_test.dart && flutter analyze lib/features/vpn_connection/presentation/widgets/iris_indicator.dart lib/features/vpn_connection/presentation/widgets/connection_timer.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../iris_indicator_test.dart` зелёный: пять статусов пампятся без исключений, disableAnimations даёт статичный кадр (нет висящих таймеров), ConnectionTimer(null) → SizedBox.
    - MediaQuery читается не в initState; все AnimationController диспоузятся; Timer отменяется.
    - Semantics liveRegion объявлен на ирисе; ConnectionTimer в FittedBox с tabular-стилем.
    - `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет | Чистая презентация; недоверенный ввод границ не пересекает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Denial of Service | AnimationController / Timer | mitigate | Все контроллеры и Timer диспоузятся; loop-анимации отключаются при disableAnimations (нет утечки тикеров) |
</threat_model>

<verification>
`flutter test test/features/vpn_connection/presentation/widgets/iris_indicator_test.dart` зелёный; `flutter analyze` виджетов чист; полный `flutter test` остаётся зелёным.
</verification>

<success_criteria>
Ирис рисует пять статусов одним CustomPainter, анимации уважают reduce-motion, таймер тикает от connectedSince и переживает пересоздание; Semantics liveRegion присутствует.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-05-SUMMARY.md` по завершении.
</output>
