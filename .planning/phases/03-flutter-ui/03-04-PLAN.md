---
phase: 03-flutter-ui
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - lib/features/vpn_connection/presentation/formatters/byte_format.dart
  - lib/features/vpn_connection/presentation/formatters/duration_format.dart
  - lib/features/vpn_logs/presentation/bloc/logs_state.dart
  - lib/features/vpn_logs/presentation/bloc/logs_cubit.dart
  - test/features/vpn_connection/presentation/format_bytes_test.dart
  - test/features/vpn_connection/presentation/timer_format_test.dart
  - test/features/vpn_logs/presentation/bloc/logs_cubit_test.dart
autonomous: true
requirements: [UI-03, UI-04, UI-05, UI-08]

must_haves:
  truths:
    - "formatBytes даёт целое для B и одну десятичную от KB (812 B, 12.4 MB), делитель 1024"
    - "hhmmss делает zero-pad и корректно показывает часы >0"
    - "LogsCubit аккумулирует записи с cap 500 (дроп самого старого), держит флаг autoScroll, отдаёт plainText в формате HH:mm:ss [LEVEL] text"
  artifacts:
    - path: "lib/features/vpn_connection/presentation/formatters/byte_format.dart"
      provides: "чистая функция formatBytes (UI-05)"
      contains: "formatBytes"
    - path: "lib/features/vpn_connection/presentation/formatters/duration_format.dart"
      provides: "чистая функция hhmmss (UI-04)"
      contains: "hhmmss"
    - path: "lib/features/vpn_logs/presentation/bloc/logs_cubit.dart"
      provides: "LogsCubit — append+cap+autoScroll+plainText"
      contains: "class LogsCubit extends Cubit"
    - path: "lib/features/vpn_logs/presentation/bloc/logs_state.dart"
      provides: "LogsState(entries, autoScroll)"
      contains: "class LogsState extends Equatable"
  key_links:
    - from: "logs_cubit.dart"
      to: "watchLogs().listen"
      via: "подписка при создании, cap 500"
      pattern: "watchLogs\\(\\).listen"
    - from: "logs_cubit.dart"
      to: "plainText"
      via: "форматирование для copy-all (UI-08)"
      pattern: "plainText"
---

<objective>
Собрать чистые форматтеры (байты, длительность) и `LogsCubit` с их unit-тестами. Это логический фундамент трафика (UI-05), таймера (UI-04) и панели логов (UI-03, UI-08); виджеты этих зон строятся поверх.

Purpose: форматтеры — чистые функции под unit-тест (не дублировать в виджетах); LogsCubit держит кольцевой буфер cap 500, флаг autoScroll (pause/resume) и plainText для copy-all. Репозиторий логов уже реплеит буфер при подписке (cap 500), Cubit аккумулирует.
Output: два форматтера + LogsState/LogsCubit + три тест-файла.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
LogEntry (lib/features/vpn_logs/domain/entities/log_entry.dart):
  enum LogLevel { info, warning, error }
  LogEntry({required String text, required LogLevel level, required DateTime time})

WatchLogs usecase (lib/features/vpn_logs/domain/usecases/watch_logs.dart):
  WatchLogs(LogRepository); Stream<LogEntry> call() => _repository.watchLogs()
  (репозиторий реплеит буфер cap 500 при подписке)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистые форматтеры байт и длительности</name>
  <files>lib/features/vpn_connection/presentation/formatters/byte_format.dart, lib/features/vpn_connection/presentation/formatters/duration_format.dart, test/features/vpn_connection/presentation/format_bytes_test.dart, test/features/vpn_connection/presentation/timer_format_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Code Examples: Форматирование байт; Форматирование таймера)
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### TrafficPanel — авто-единица; ### ConnectionTimer — HH:MM:SS)
  </read_first>
  <behavior>
    - formatBytes(812) == '812 B'; formatBytes(0) == '0 B'
    - formatBytes(1024) == '1.0 KB'; formatBytes(12_400_000)≈'11.8 MB' (делитель 1024, одна десятичная от KB)
    - formatBytes на границе GB даёт '.. GB'; целое только для B
    - hhmmss(Duration(seconds:5)) == '00:00:05'; hhmmss(Duration(hours:1, minutes:2, seconds:3)) == '01:02:03'
    - hhmmss с часами >0 не теряет часы; zero-pad всех компонент
  </behavior>
  <action>
    Написать сперва тесты `format_bytes_test.dart` и `timer_format_test.dart` (RED) по `<behavior>`.

    Затем `byte_format.dart`: `String formatBytes(int bytes)` — единицы B/KB/MB/GB, делитель 1024, целое для B (<1024), одна десятичная (`toStringAsFixed(1)`) от KB и выше. `duration_format.dart`: `String hhmmss(Duration d)` — zero-pad `padLeft(2,'0')` для h/m/s, часы из `d.inHours`, минуты `d.inMinutes % 60`, секунды `d.inSeconds % 60`. Обе — чистые top-level функции, без сайд-эффектов. Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/format_bytes_test.dart test/features/vpn_connection/presentation/timer_format_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Оба тест-файла зелёные по всем кейсам `<behavior>` (включая границу 1024 и часы >0).
    - formatBytes и hhmmss — чистые функции; `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LogsCubit + LogsState</name>
  <files>lib/features/vpn_logs/presentation/bloc/logs_state.dart, lib/features/vpn_logs/presentation/bloc/logs_cubit.dart, test/features/vpn_logs/presentation/bloc/logs_cubit_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 4: LogsCubit с cap и autoScroll-флагом)
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### LogsCubit — cap 500, pause/resume, plainText формат)
  </read_first>
  <behavior>
    - Приём записи append в конец; при >500 дропается самый старый (длина остаётся 500, первый элемент = 501-й пришедший)
    - autoScroll по умолчанию true; pauseAutoScroll() → false; resumeAutoScroll() → true; повторный вызов идемпотентен
    - plainText() форматирует каждую запись как 'HH:mm:ss [LEVEL] text' (LEVEL uppercase), строки через \n
  </behavior>
  <action>
    Написать сперва тест `logs_cubit_test.dart` (RED): подавать записи через фейковый/мок WatchLogs (StreamController<LogEntry>), проверить append, cap 500 (подать 501+ записей), pause/resume autoScroll, формат plainText. Мок WatchLogs определить в самом тест-файле (mocktail или мини-fake со StreamController), не создавая общего хелпера.

    Затем `logs_state.dart`: `class LogsState extends Equatable` с `List<LogEntry> entries` (const []) и `bool autoScroll` (true), copyWith, props. `logs_cubit.dart`: `class LogsCubit extends Cubit<LogsState>` — конструктор инъектит `WatchLogs watchLogs`, подписывается `_sub = watchLogs().listen(_append)`. `_append`: собрать `[...state.entries, entry]`, при длине >500 срезать голову (`removeRange(0, len-500)`), emit. `pauseAutoScroll()`/`resumeAutoScroll()` меняют флаг только при изменении. `String plainText()` — map записей в 'HH:mm:ss [LEVEL] text' через хелпер времени (zero-pad) + join('\n'). Переопределить `close()`: отменить подписку. Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_logs/presentation/bloc/logs_cubit_test.dart && flutter analyze lib/features/vpn_logs/presentation/bloc/logs_cubit.dart lib/features/vpn_logs/presentation/bloc/logs_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../logs_cubit_test.dart` зелёный: append, cap 500 (дроп головы), pause/resume, plainText формат.
    - close() отменяет подписку на watchLogs.
    - `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| логи native → LogsCubit → буфер обмена | Строки логов из собственного native-сервиса; plainText уходит в системный clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | LogsCubit.plainText (copy-all) | mitigate | plainText форматирует только видимые строки логов (LogEntry.text/level/time); VpnConfig.userId/секреты в логи не попадают (native их не логирует, решение 02-03); конфиг в буфер не копируется |
| T-3-05 | Denial of Service | буфер логов | mitigate | Cap 500 с дропом головы — рост буфера ограничен |
</threat_model>

<verification>
Три тест-файла зелёные; весь `flutter test` остаётся зелёным; `flutter analyze` чист.
</verification>

<success_criteria>
Форматтеры байт/длительности покрыты тестами; LogsCubit держит cap 500, autoScroll-флаг и plainText; логическая база трафика/таймера/логов готова для виджетов.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-04-SUMMARY.md` по завершении.
</output>
