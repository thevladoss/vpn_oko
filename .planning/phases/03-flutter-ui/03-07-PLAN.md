---
phase: 03-flutter-ui
plan: 07
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - lib/features/vpn_logs/presentation/widgets/log_line.dart
  - lib/features/vpn_logs/presentation/widgets/log_console.dart
  - test/features/vpn_logs/presentation/widgets/log_console_test.dart
autonomous: true
requirements: [UI-03, UI-08]

must_haves:
  truths:
    - "LogConsole показывает строки логов в моноширинном шрифте, автоскроллит к новым при autoScroll и ставит паузу при ручной прокрутке вверх"
    - "LogLine красит уровень: info secondary, warning transitional, error coral"
    - "Copy-all копирует plainText в буфер и даёт SnackBar/haptic; пустой буфер показывает состояние 'Waiting for events'"
  artifacts:
    - path: "lib/features/vpn_logs/presentation/widgets/log_console.dart"
      provides: "DraggableScrollableSheet + автоскролл + copy-all + empty state"
      contains: "class LogConsole"
      min_lines: 40
    - path: "lib/features/vpn_logs/presentation/widgets/log_line.dart"
      provides: "одна строка лога, цвет по уровню, JetBrains Mono"
      contains: "class LogLine"
  key_links:
    - from: "log_console.dart"
      to: "context.read<LogsCubit>()"
      via: "pauseAutoScroll/resumeAutoScroll/plainText"
      pattern: "LogsCubit"
    - from: "log_console.dart"
      to: "Clipboard.setData"
      via: "copy-all"
      pattern: "Clipboard.setData"
    - from: "log_line.dart"
      to: "LogLevel цвет"
      via: "маппинг уровня в accent/secondary"
      pattern: "LogLevel"
---

<objective>
Построить панель логов: `DraggableScrollableSheet` с живыми строками, цветовой маркировкой уровня, автоскроллом с отключением при ручной прокрутке и копированием всего лога.

Purpose: UI-03 (реалтайм-логи, буфер, автоскролл с отключением), UI-08 (уровни info/warning/error цветом + copy-all). LogConsole читает LogsCubit (buffer/autoScroll/plainText из плана 03-04) через BlocConsumer.
Output: log_line, log_console + widget-тест.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md

<interfaces>
LogEntry: lib/features/vpn_logs/domain/entities/log_entry.dart — { text, LogLevel level (info/warning/error), DateTime time }
LogsCubit / LogsState: lib/features/vpn_logs/presentation/bloc/ — LogsState{entries, autoScroll}; методы pauseAutoScroll(), resumeAutoScroll(), plainText()
OkoTones: accentTransitional (warning), accentError (error), textSecondary (info + время)
OkoMotion.autoscroll (200ms easeOut)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LogLine + LogConsole</name>
  <files>lib/features/vpn_logs/presentation/widgets/log_line.dart, lib/features/vpn_logs/presentation/widgets/log_console.dart, test/features/vpn_logs/presentation/widgets/log_console_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### LogConsole + LogLine — sheet, автоскролл, copy-all, empty state; ## Accessibility — LogConsole)
    - .planning/phases/03-flutter-ui/03-RESEARCH.md (Pattern 8: DraggableScrollableSheet + автоскролл; Pitfall 6: animateTo до layout)
  </read_first>
  <behavior>
    - LogConsole с cubit, содержащим записи info/warning/error, рендерит по строке на запись (find LogLine == entries.length)
    - LogLine уровня warning окрашен в accentTransitional; error в accentError; info в textSecondary
    - Пустой буфер показывает 'Waiting for events' (empty state), а не пустой прямоугольник
    - Тап copy-all вызывает Clipboard.setData (проверяется через мок SystemChannels/handler)
  </behavior>
  <action>
    Создать `log_line.dart` — `class LogLine extends StatelessWidget` с `final LogEntry entry`: `Text` JetBrains Mono 12, формат 'HH:mm:ss  text' (время всегда secondary). Цвет текста ведёт уровень: info→textSecondary, warning→accentTransitional, error→accentError (UI-08). Semantics(label: '{level}: {text}').

    Создать `log_console.dart` — `class LogConsole extends StatelessWidget`: `DraggableScrollableSheet(initialChildSize: 0.12, minChildSize: 0.12, maxChildSize: 0.70, builder: (context, scrollController) {...})`. Фон surfaceElevated, верхние углы radius 24. Шапка: grabber (4x36 secondary, центр), слева заголовок 'Logs' (title 20/600, Semantics heading), справа copy-all кнопка (`Icons.copy_rounded`, touch 48, Semantics 'Copy all logs'). Тело: `NotificationListener<ScrollNotification>` вокруг `BlocConsumer<LogsCubit, LogsState>`. listener: при `state.autoScroll && scrollController.hasClients` — `WidgetsBinding.instance.addPostFrameCallback` затем `animateTo(maxScrollExtent, OkoMotion.autoscroll)` (Pitfall 6: только после layout). builder: если entries пуст — empty state (иконка `Icons.terminal_rounded` secondary 32 + 'Waiting for events' body + 'Tunnel logs stream here in real time.' caption); иначе `ListView.builder(controller: scrollController, ...LogLine)`. Свой ScrollController заводить нельзя — использовать выданный sheet-контроллер (Pattern 8). NotificationListener: если пользователь ушёл от maxScrollExtent > порога → `context.read<LogsCubit>().pauseAutoScroll()`, у низа → resumeAutoScroll(). Copy-all: `Clipboard.setData(ClipboardData(text: context.read<LogsCubit>().plainText()))` затем `HapticFeedback.selectionClick()` и SnackBar 'Copied {n} lines'.

    Написать `log_console_test.dart` (RED сначала) по `<behavior>`: обернуть LogConsole в BlocProvider<LogsCubit> (с мок/fake WatchLogs, StreamController подаёт записи) внутри MaterialApp(theme: OkoTheme.dark) в достаточном по высоте Scaffold; проверить рендер строк, цвета уровней, empty state, вызов Clipboard (перехватить через tester.binding.defaultBinaryMessenger mock SystemChannels.platform). Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_logs/presentation/widgets/log_console_test.dart && flutter analyze lib/features/vpn_logs/presentation/widgets/log_line.dart lib/features/vpn_logs/presentation/widgets/log_console.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../log_console_test.dart` зелёный: строки рендерятся, цвета уровней (info/warning/error) верны, empty state 'Waiting for events', copy-all вызывает Clipboard.setData.
    - Используется выданный DraggableScrollableSheet-контроллер (без своего ScrollController); animateTo только после layout (hasClients + postFrame).
    - Цвета из OkoTones (grep не находит Color(0xFF); `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| логи native → LogConsole → буфер обмена/экран | Строки логов из собственного native; рендер и copy-all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | copy-all → Clipboard | mitigate | Копируется только plainText видимых строк логов; VpnConfig.userId/секреты в логи не попадают (native не логирует, решение 02-03) |
| T-3-08 | Tampering | LogLine рендер строки лога/ошибки | accept | Flutter Text не исполняет разметку; строки из собственного native, не из внешней сети |
</threat_model>

<verification>
`flutter test test/features/vpn_logs/presentation/widgets/log_console_test.dart` зелёный; `flutter analyze` виджетов чист; полный `flutter test` остаётся зелёным.
</verification>

<success_criteria>
Панель логов рендерит живые строки с цветами уровней, автоскроллит с отключением при ручной прокрутке, копирует весь лог, показывает осмысленное пустое состояние.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-07-SUMMARY.md` по завершении.
</output>
