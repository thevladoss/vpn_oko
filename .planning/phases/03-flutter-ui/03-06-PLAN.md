---
phase: 03-flutter-ui
plan: 06
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - lib/features/vpn_connection/presentation/widgets/oko_wordmark.dart
  - lib/features/vpn_connection/presentation/widgets/status_badge.dart
  - lib/features/vpn_connection/presentation/widgets/server_card.dart
  - lib/features/vpn_connection/presentation/widgets/traffic_tile.dart
  - lib/features/vpn_connection/presentation/widgets/traffic_panel.dart
  - lib/features/vpn_connection/presentation/widgets/connect_button.dart
  - test/features/vpn_connection/presentation/widgets/status_badge_test.dart
  - test/features/vpn_connection/presentation/widgets/connect_button_test.dart
autonomous: true
requirements: [UI-01, UI-02, UI-04, UI-05, UI-06]

must_haves:
  truths:
    - "StatusBadge показывает слово статуса для каждого из пяти статусов, цвет из accentFor"
    - "ConnectButton в connecting/disconnecting имеет onPressed==null (двойной тап невозможен на уровне UI), лейбл ведёт статус"
    - "TrafficPanel показывает rx/tx через formatBytes; ServerCard показывает имя и host:port"
  artifacts:
    - path: "lib/features/vpn_connection/presentation/widgets/connect_button.dart"
      provides: "кнопка Connect/Disconnect/Retry, блокировка в переходных, haptic"
      contains: "class ConnectButton"
    - path: "lib/features/vpn_connection/presentation/widgets/status_badge.dart"
      provides: "pill-бейдж со словом статуса, кроссфейд цвета"
      contains: "class StatusBadge"
    - path: "lib/features/vpn_connection/presentation/widgets/traffic_panel.dart"
      provides: "ряд из двух TrafficTile (rx/tx)"
      contains: "class TrafficPanel"
    - path: "lib/features/vpn_connection/presentation/widgets/server_card.dart"
      provides: "карточка сервера name + host:port"
      contains: "class ServerCard"
    - path: "lib/features/vpn_connection/presentation/widgets/oko_wordmark.dart"
      provides: "wordmark oko"
      contains: "class OkoWordmark"
  key_links:
    - from: "traffic_tile.dart"
      to: "formatBytes"
      via: "значение плитки"
      pattern: "formatBytes"
    - from: "connect_button.dart"
      to: "onPressed null в переходных"
      via: "isBusy-статус"
      pattern: "onPressed"
    - from: "status_badge.dart"
      to: "OkoTones.accentFor(status)"
      via: "цвет бейджа"
      pattern: "accentFor"
---

<objective>
Собрать хром и элементы управления экрана: wordmark, бейдж статуса, карточку сервера, плитки трафика и кнопку Connect/Disconnect. Это презентационные виджеты (принимают примитивы + колбэки), которые экран свяжет с Bloc.

Purpose: UI-01 (бейдж-слово статуса), UI-02 (кнопка блокируется в переходных, onPressed==null + haptic), UI-04 (карточка сервера), UI-05 (плитки rx/tx через formatBytes), UI-06 (стили из OkoTones, никакого хардкода цветов, никаких спиннеров — прогресс бегущим сегментом).
Output: шесть виджетов + два widget-теста (StatusBadge слово, ConnectButton блокировка).
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flutter-ui/03-UI-SPEC.md
@.planning/phases/03-flutter-ui/03-RESEARCH.md

<interfaces>
VpnStatus: lib/core/theme/vpn_status.dart
OkoTones: Theme.of(context).extension<OkoTones>()! — accentFor(status), surfaceCard, surfaceElevated, textPrimary, textSecondary
OkoMotion.statusCrossfade (300ms) для кроссфейда цвета
formatBytes: lib/features/vpn_connection/presentation/formatters/byte_format.dart
TextTheme из OkoTheme (Space Grotesk display/title, Inter body/label)

<design_note>
Виджеты презентационные: принимают статус/данные + колбэки (onConnect/onDisconnect), НЕ читают Bloc напрямую. Экран (план 03-08) оборачивает их в BlocBuilder<VpnConnectionBloc> и шлёт ConnectRequested/DisconnectRequested. Это упрощает widget-тесты и держит виджеты чистыми. Иконки — Material Symbols rounded (Icons.*_rounded), без новых зависимостей.
</design_note>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OkoWordmark, StatusBadge, ServerCard</name>
  <files>lib/features/vpn_connection/presentation/widgets/oko_wordmark.dart, lib/features/vpn_connection/presentation/widgets/status_badge.dart, lib/features/vpn_connection/presentation/widgets/server_card.dart, test/features/vpn_connection/presentation/widgets/status_badge_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### OkoWordmark; ### StatusBadge; ### ServerCard; ## Copywriting Contract — слова статусов)
  </read_first>
  <behavior>
    - StatusBadge(disconnected) содержит текст 'Disconnected'; connecting→'Connecting'; connected→'Connected'; disconnecting→'Disconnecting'; error→'Error'
    - StatusBadge окрашивает текст/фон из accentFor(status) (не хардкод)
  </behavior>
  <action>
    Создать `oko_wordmark.dart` — `class OkoWordmark extends StatelessWidget` с `final VpnStatus status`: текст 'oko' стилем title (Space Grotesk 20/600, textPrimary), опциональная акцентная точка над 'o' цветом accentFor(status). Без изображений.

    Создать `status_badge.dart` — `class StatusBadge extends StatelessWidget` с `final VpnStatus status`: pill (radius 999), фон `accentFor(status)` @ opacity 0.16, текст слова статуса (label 12/600 uppercase) цветом accentFor(status). Слова: Disconnected/Connecting/Connected/Disconnecting/Error. Цвет кроссфейдит через AnimatedContainer/AnimatedDefaultTextStyle за OkoMotion.statusCrossfade. Semantics бейджа исключить из общего узла ириса (excludeSemantics) — озвучивание статуса на ирисе.

    Создать `server_card.dart` — `class ServerCard extends StatelessWidget` с `final String serverName, String host, int port`: контейнер surface (radius 20, паддинг lg), иконка `Icons.dns_rounded` (secondary), две строки — serverName (body 15/600 primary) и '$host:$port' (caption 12 secondary), справа `Icons.chevron_right_rounded` (secondary, статичный).

    Написать `status_badge_test.dart` (RED сначала) по `<behavior>`: pump StatusBadge для каждого статуса в MaterialApp(theme: OkoTheme.dark), assert `find.text(word)`. Комментарии запрещены; цвета только из OkoTones.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/widgets/status_badge_test.dart && flutter analyze lib/features/vpn_connection/presentation/widgets/oko_wordmark.dart lib/features/vpn_connection/presentation/widgets/status_badge.dart lib/features/vpn_connection/presentation/widgets/server_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../status_badge_test.dart` зелёный: правильное слово для всех пяти статусов.
    - Цвета берутся из OkoTones.accentFor (grep не находит Color(0xFF в виджетах).
    - `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrafficTile, TrafficPanel, ConnectButton</name>
  <files>lib/features/vpn_connection/presentation/widgets/traffic_tile.dart, lib/features/vpn_connection/presentation/widgets/traffic_panel.dart, lib/features/vpn_connection/presentation/widgets/connect_button.dart, test/features/vpn_connection/presentation/widgets/connect_button_test.dart</files>
  <read_first>
    - .planning/phases/03-flutter-ui/03-UI-SPEC.md (### TrafficPanel + TrafficTile; ### ConnectButton — таблица label/стиль/enabled; ## Copywriting Contract; ## Accessibility — TrafficTile/ConnectButton Semantics)
    - lib/features/vpn_connection/presentation/formatters/byte_format.dart
  </read_first>
  <behavior>
    - ConnectButton(connecting).onPressed == null; ConnectButton(disconnecting).onPressed == null
    - ConnectButton(disconnected) лейбл 'Connect' и onPressed != null; connected → 'Disconnect'; error → 'Retry'
    - Тап ConnectButton в disconnected вызывает переданный колбэк
  </behavior>
  <action>
    Создать `traffic_tile.dart` — `class TrafficTile extends StatelessWidget` с полями: направление (rx/tx), `int bytes`, `bool active`. Плитка surface (radius 16, высота 72, паддинг md): верх — иконка направления (`Icons.arrow_downward_rounded`/`Icons.arrow_upward_rounded`) + uppercase-лейбл ('DOWN'/'UP'), accentConnected при active иначе secondary; низ — значение `formatBytes(bytes)` (значение Space Grotesk 20/600 tabular primary, единица Inter 12 secondary — разбить строку formatBytes на число+единицу). Semantics 'Downloaded {value}'/'Uploaded {value}'.

    Создать `traffic_panel.dart` — `class TrafficPanel extends StatelessWidget` с `final int rxBytes, txBytes; final VpnStatus status`: `Row` из двух TrafficTile равной ширины (Expanded), gap md; active = (status==connected).

    Создать `connect_button.dart` — `class ConnectButton extends StatelessWidget` с `final VpnStatus status; final VoidCallback? onConnect; final VoidCallback? onDisconnect`. Full-width, высота 56, radius 16. По статусу (см. UI-SPEC таблицу): disconnected → filled accentConnected, label 'Connect', leading power icon, onPressed=onConnect; connected → tonal/outlined accentError, 'Disconnect', onPressed=onDisconnect; error → filled accentError tonal, 'Retry', leading refresh, onPressed=onConnect; connecting/disconnecting → filled accentTransitional @0.6, label 'Connecting…'/'Disconnecting…', `onPressed: null` (блокировка), inline бегущий сегмент-progress (НЕ CircularProgressIndicator — спиннеры запрещены). На тап (когда enabled) — `HapticFeedback.mediumImpact()` затем колбэк. Стиль кроссфейдит за statusCrossfade. Semantics: label=действие, button:true, enabled отражает блокировку.

    Написать `connect_button_test.dart` (RED сначала) по `<behavior>`: pump ConnectButton для connecting/disconnecting (assert onPressed==null через tester tap не срабатывает / find button disabled), для disconnected/connected/error (правильный лейбл), тап disconnected вызывает колбэк. Комментарии запрещены.
  </action>
  <verify>
    <automated>flutter test test/features/vpn_connection/presentation/widgets/connect_button_test.dart && flutter analyze lib/features/vpn_connection/presentation/widgets/traffic_tile.dart lib/features/vpn_connection/presentation/widgets/traffic_panel.dart lib/features/vpn_connection/presentation/widgets/connect_button.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test .../connect_button_test.dart` зелёный: onPressed==null в connecting/disconnecting (UI-02), правильные лейблы Connect/Disconnect/Retry, тап вызывает колбэк.
    - Никаких CircularProgressIndicator/спиннеров (прогресс — бегущий сегмент).
    - Цвета из OkoTones (grep не находит Color(0xFF); значения трафика через formatBytes.
    - `flutter analyze` без варнингов; ноль комментариев.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет | Презентационные виджеты; недоверенный ввод границ не пересекает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Tampering | вторая линия защиты двойного тапа | mitigate | onPressed==null в переходных статусах (UI-уровень) дублирует Bloc-гейт isBusy (план 03) |
</threat_model>

<verification>
Оба widget-теста зелёные; `flutter analyze` виджетов чист; полный `flutter test` остаётся зелёным; хардкод Color в виджетах отсутствует.
</verification>

<success_criteria>
Хром и управление собраны: бейдж-слово статуса, карточка сервера, плитки rx/tx, кнопка с блокировкой в переходных и haptic; все цвета из OkoTones; двойной тап невозможен на уровне UI.
</success_criteria>

<output>
Создать `.planning/phases/03-flutter-ui/03-06-SUMMARY.md` по завершении.
</output>
